# 경매 참고 화면과 현재 구현 매핑

> 과거 원자료·일회성 스크립트는 사용자의 정리 요청으로 삭제되었습니다. 인라인 경로는 과거 이력 표기이며, 최신 근거는 [Riot 연동 보고](RIOT_API_REPORT.md)에서 확인합니다.

작성 기준: 2026-09-08. 최신 요청으로 전달된 화면 요소와 새 실행 경로를 대조했다. 아래에서 **반영**은 코드와 명시한 집중 검증의 완료를 뜻한다. 최신 첨부 이미지의 픽셀 배치나 실제 브라우저 표시를 직접 검수했다는 뜻은 아니다. 전체 최종 회귀는 별도 결과로 기록하며 이전 리허설의 검증 결과도 이력으로 남긴다.

## 화면 요소별 대응

| 참고 화면·요청 요소 | 현재 구현과 데이터 출처 | 처리 방향 |
|---|---|---|
| 왼쪽 팀 현황 | 팀 선택, 이전/다음 팀 화살표, 팀장, 낙찰 선수, 인원과 잔액이 있다. 내 팀을 기본으로 선택하지만 모든 팀을 조회할 수 있다. [auction_ui.py:499](roly/auction_ui.py#L499), [auction_components.py:494](roly/auction_components.py#L494) | **반영.** 팀을 조회하는 선택은 입찰 팀 변경이 아니다. 입찰은 로그인 회원이 맡은 해당 경매의 팀으로만 처리한다. |
| 상단 현재 선수 → 다음 선수 | 상단 순서 띠에 현재·다음 대상과 화살표를 표시한다. 회원별 최신 lot을 골라 과거 재경매 행의 중복을 제거한다. [auction_components.py:582](roly/auction_components.py#L582), [auction_ui.py:468](roly/auction_ui.py#L468) | **반영.** 저장된 순서에서 첫 `QUEUED` 선수를 표시한다. 마지막 선수·유찰 재시작 대기에 가짜 다음 선수를 만들지 않고, 화면 갱신 때 다시 추첨하지 않는다. |
| 현재 경매 대상의 닉네임·Riot ID·프로필 | 확정 명단의 Riot ID와 현재 동일 ID에 연결된 공개 Riot 캐시를 사용한다. 이미지 URL 검증·실패 시 글자 대체가 있다. [auction_components.py:459](roly/auction_components.py#L459), [riot_profile.py:65](roly/riot_profile.py#L65) | **유지.** 이름 변경 후 이전 명단에 다른 ID의 API 정보를 붙이지 않는다. 이미지 실패가 선수명이나 입찰 버튼을 가리지 않아야 한다. |
| 클랜 티어·현재 티어 | 클랜 티어는 명단 확정 스냅샷, 현재 솔로·현재 자유 티어는 각 API 캐시 값으로 구분한다. 솔로 미조회 시 명단 당시 정보라는 설명을 붙인다. [auction_components.py:459](roly/auction_components.py#L459) | **반영.** API 미조회와 실제 언랭크를 구분하며, 현재 API 티어를 경기 전력이나 확정 클랜 티어에 자동 대입하지 않는다. |
| 주력 챔피언 5개 | API·큐·공개 캐시·화면이 공통 `MASTERY_LIMIT=5`를 사용한다. 실제 체험 스냅샷의 성공 21명도 모두 챔피언 5개로 다시 조회했다. [riot_api.py](roly/riot_api.py), [riot_sync.py](roly/riot_sync.py), [auction_components.py:471](roly/auction_components.py#L471) | **반영.** 실제 숙련도 점수·레벨을 제공한다. 과거 캐시가 3개뿐이면 있는 데이터만 표시하고 갱신한다. 없는 2개, 챔피언별 승률·판수는 만들지 않는다. |
| 현재 최고 입찰 포인트 | 현재 lot의 `highest_bid`를 표시한다. 첫 입찰 전, 유찰, 낙찰, 낙찰 취소의 문구가 다르다. [auction_components.py:514](roly/auction_components.py#L514) | **유지·강조 개선.** 입력 중인 금액과 서버가 접수한 최고가를 분리한다. 낙찰 전 최고가를 이미 소비한 포인트로 표현하지 않는다. |
| 최고 입찰 팀·팀장 | 최고가의 `highest_team_id`에서 해당 팀과 고정 팀장을 찾아 표시한다. [auction_components.py:516](roly/auction_components.py#L516) | **유지.** 현재 왼쪽에서 보고 있는 팀과 최고 입찰 팀이 다를 수 있으므로 각각 이름을 표시한다. |
| 포인트 증액·입찰 버튼 | 내 잔여 포인트와 입력 포인트를 두 칸으로 구분한다. `+5/+10/+20/+30/+50/+70/+100`과 금액 초기화가 8개 조작 버튼이며, 별도로 최종 입찰 버튼이 있다. 증액은 현재 입력값과 최고가 중 큰 값에 더한다. [live_auction.py:21](roly/live_auction.py#L21), [auction_ui.py:544](roly/auction_ui.py#L544) | **반영.** 증액만으로 입찰이 접수되지는 않는다. 금액 초기화는 입력 초안만 현재 최고가로 되돌린다. 최종 버튼의 표시 금액과 서버 제출 금액이 달라지면 거절한다. |
| 팀별 시작·잔여 포인트 | 시작 전 모든 팀의 예산을 저장할 수 있다. 기본 예산은 팀장 전력 구간으로 산정하며, 잔액은 예산에서 실제 낙찰가 합계를 뺀 값이다. [tournament.py:179](roly/tournament.py#L179), [live_auction.py:107](roly/live_auction.py#L107), [live_auction.py:779](roly/live_auction.py#L779) | **유지.** 현재 Riot 티어·챔피언 숙련도에 따라 예산을 실시간으로 바꾸지 않는다. 시작 후 설정 변경도 허용하지 않는다. |
| 큰 타이머·게이지 | 서버 마감 시각과 남은 시간을 사용하며, 클라이언트는 그 사이 표시만 갱신한다. 같은 마감의 늦은 응답 때문에 시간이 반복 증가하지 않도록 기준 시각을 유지한다. [live_auction.py:772](roly/live_auction.py#L772), [auction_components.py:396](roly/auction_components.py#L396) | **유지·위치 개선.** 선택 시간 5/10/15/20/25/30초, 기본 10초. 새 유효 입찰은 남은 시간 +5초이되 선택한 시간을 넘지 않는다. 중복·거절된 입찰은 연장하지 않는다. |
| 낙찰·유찰 후 다음 선수 준비 | 낙찰 또는 유찰을 먼저 확정하고 다음 선수까지 3초를 둔다. 해당 대기 중 입찰 버튼은 비활성이다. [live_auction.py:187](roly/live_auction.py#L187), [auction_components.py:521](roly/auction_components.py#L521) | **유지.** 다음 선수 미리보기는 준비 시간 중의 입찰 권한을 만들지 않는다. |
| 잔여 선수·전체 진행 상황 | 상단에 이번 순서의 `OPEN+QUEUED` 인원과 유찰 인원을 구분한다. 아래에는 입찰 대기·유찰 대기와 포지션별 수량을 표시한다. [auction_components.py:582](roly/auction_components.py#L582), [auction_components.py:610](roly/auction_components.py#L610) | **반영.** 회원별 최신 lot과 현재 참가·배정 상태를 함께 검사한다. 재경매 이력 전체 행수를 선수 수로 세지 않는다. 하단 대기 명단에는 현재 입찰 중인 선수를 중복 표시하지 않는다. |
| 전체 경매 현황 팝업의 포지션 행 | 팀별 탑/정글/미드/원딜/서포터 행, 팀장 표시, 인원·잔액과 내 팀 구분을 제공한다. [auction_components.py:494](roly/auction_components.py#L494), [auction_components.py:561](roly/auction_components.py#L561) | **반영.** 같은 포지션 선수가 여럿이면 그 행에 전원을 표시한다. 이것은 조회 방식 변경이며, 입찰 중 포지션별 정원을 새로 강제하지 않는다. |
| 입찰 이력·운영 이력 | 오른쪽 영역에 최근 30개 유효 경매 대상의 입찰과 운영 이력이 있다. `CANCELLED` 대상의 입찰은 ‘초기화·취소 전 입찰 기록’으로 분리하고 DB 원장은 보존한다. [auction_ui.py:586](roly/auction_ui.py#L586), [live_auction.py:756](roly/live_auction.py#L756) | **유지·분리 개선.** 화면 표시 개수와 DB 보존 개수를 구분한다. 낙찰 정정·재경매 뒤에도 기존 접수 이력을 새 입찰로 바꾸지 않는다. |
| 일시정지·재개·유찰 재시작 | 주최자·관리자에게만 노출되고 서버에서도 권한을 검사한다. 유찰 재시작은 이번 순서를 마친 뒤 유찰 전원을 다시 섞는다. [auction_ui.py:475](roly/auction_ui.py#L475), [live_auction.py:412](roly/live_auction.py#L412) | **유지.** 일반 관전자에게 운영 버튼을 추가하지 않는다. 일시정지 중 입찰은 차단하고, 재개 시 남은 시간부터 진행한다. |
| 낙찰 오류 수정 | 관리자에게 환불·팀/가격 변경 미리보기와 사유를 요구한다. 진행 중에는 일시정지 상태, 경매 완료 후에는 대진 생성 전만 가능하다. [auction_ui.py:204](roly/auction_ui.py#L204), [live_auction.py:555](roly/live_auction.py#L555) | **유지.** 화면에서 선수를 드래그하는 동작으로 낙찰 팀·가격·원장을 우회 변경하지 않는다. |
| 오른쪽 상단 소리·음량 | 현재 선수 카드와 분리한 상단 스피커 아이콘에서 켜기·음량을 조절한다. 사용자 조작 이후 새로 접수된 입찰에 반응한다. [auction_components.py:551](roly/auction_components.py#L551), [auction_ui.py:465](roly/auction_ui.py#L465) | **반영.** 최초 조회·중복 응답·거절 입찰에 새 입찰 소리를 만들지 않는다. 초기화·취소로 `CANCELLED`가 된 대상의 입찰은 소리 신호에서도 제외하므로 브라우저가 중간 준비 상태를 놓쳐도 과거 입찰이 울리지 않는다. 브라우저가 허용하지 않은 자동 재생을 전제로 하지 않는다. |
| 오른쪽 상단 경매 초기화 | 진행 화면 상단뿐 아니라 설정이 저장된 준비 완료 화면과 대진 생성 전 경매 완료 화면에서도 주최자·관리자가 같은 초기화 창을 연다. 환불·잔액 미리보기와 사유를 확인하고, 참가자·고정 팀장·시작 예산·입찰 시간을 유지한 채 시작 대기로 돌아간다. [auction_ui.py:135](roly/auction_ui.py#L135), [auction.py:168](app_pages/auction.py#L168), [live_auction.py:489](roly/live_auction.py#L489) | **반영.** 낙찰 배정은 취소하고 포인트를 돌려주며 선수 순서를 새로 섞는다. 이전 입찰·낙찰 내용은 감사 이력으로 보존한다. 미리보기 이후 변경은 거절하고 같은 UUID 재시도는 중복 실행하지 않는다. 대진·실경기·우승 보상이 생긴 이후에는 차단한다. 금액 입력 초기화와는 별개이며 설정·정정·전체 팀 팝업과 동시에 열리지 않는다. |

## 포지션 표시에서 구분할 값

| 값 | 의미 | 표시·변경 원칙 |
|---|---|---|
| 회원 `main_role`, `sub_role` | 현재 회원 정보에 등록한 주·부 포지션 | 선수의 선호 정보를 설명한다. 이 값이 바뀌었다고 진행 경매의 확정 명단을 다시 배정하지 않는다. |
| 명단 `main_role_snapshot`, `sub_role_snapshot` | 명단 확정 당시 회원의 주·부 포지션 | 해당 경매의 참고 정보로 사용한다. [tournament.py:160](roly/tournament.py#L160) |
| 명단 `role` | 이번 경기에서 배정한 포지션 | 팀 현황의 포지션 행은 이 값을 기준으로 삼고, 주포지션과 혼동되지 않게 표시한다. |

경매에서는 참가자 확정이나 입찰마다 포지션별 한 명을 강제하지 않는다. 다만 **경매가 끝난 뒤 대진을 만들기 전에는 팀당 5명과 5개 포지션을 확인**한다. 주최자가 기존 팀 포지션 저장 화면에서 정리하며 서버에서도 중복을 검사한다. [tournament.py:151](roly/tournament.py#L151), [tournament.py:260](roly/tournament.py#L260), [tournament.py:340](roly/tournament.py#L340), [auction.py:183](app_pages/auction.py#L183)

시작 대기 화면은 첫 선수를 미리 보여주더라도 **시작 대기 / 설정 N초**로 표시한다. 가짜 `0초` 마감이나 동작 중인 게이지를 만들지 않으며 시작 전 입찰은 허용하지 않는다. [auction_components.py:514](roly/auction_components.py#L514)

참고 화면처럼 포지션 행으로 보여주는 것과, 경매 중 누구나 선수를 끌어 팀이나 포지션을 변경하는 것은 다른 기능이다. 후자는 이번 화면 요청으로 운영 권한을 추가하거나 고정 팀장을 교체하는 근거가 되지 않는다.

## 그대로 가져오지 않는 동작과 대체 방법

| 참고 방식 또는 오해할 수 있는 동작 | 다르게 유지하는 이유 | 현재 대체 방법 |
|---|---|---|
| 포지션별 고정 순서로 경매하기 | 사용자가 무작위 순서를 확정했다. | 최초 무작위 순서를 DB에 저장하고 재접속·설정 재저장 시 유지한다. 유찰 재시작은 그때 유찰 선수만 다시 섞는다. [live_auction.py:149](roly/live_auction.py#L149) |
| 조회용 포지션 정렬이 입찰 순서도 변경 | 조회 정렬과 서버의 다음 lot 결정이 섞이면 다시 추첨한 것과 같은 혼선이 생긴다. | 선수 목록을 포지션별로 보더라도 `sequence`와 다음 대상은 유지한다. |
| 모든 관전자에게 입찰·진행 제어 제공 | 관전 범위는 전체 경매지만 입찰자는 해당 경매의 지정 팀장이다. | 모든 회원이 전체 팀·최고가·선수·기록을 함께 조회한다. 주최자/관리자는 운영, 팀장은 본인 팀 입찰을 맡는다. 한 사람이 주최자이자 팀장일 수 있다. [live_auction.py:215](roly/live_auction.py#L215) |
| 경매 중 팀장 교체 | 사용자가 경매별 팀장을 시작 후 고정하도록 정했다. | 시작 전에는 명단 재준비와 팀장 재지정, 시작 후 계정 문제는 일시정지 후 계정 상태 복구를 사용한다. [tournament.py:168](roly/tournament.py#L168) |
| 참고 이미지의 임의 승률·주력 챔피언 숫자 복제 | 실제 숙련도와 챔피언별 승률은 서로 다른 데이터다. | 실제 API의 상위 숙련도 챔피언·점수·레벨을 표시한다. 조회 실패는 미조회 또는 마지막 성공 데이터로 표시한다. |
| 화면을 보고 있었던 클라이언트가 낙찰을 결정 | 탭이 숨겨지거나 연결이 늦으면 잘못된 낙찰·중복 처리로 이어질 수 있다. | 서버 마감·트랜잭션에서 결정하고 화면은 결과를 조회한다. |

## 0.5초 갱신과 실제 반응 시간

- 본 경매 화면은 `run_every=0.5`로 조회한다. 전체 팀 팝업과 시작 대기 감시는 1초로 조회한다. 이는 **조회 주기**이며 모든 사용자에게 결과가 0.5초 안에 보인다는 보장은 아니다. [auction_ui.py:400](roly/auction_ui.py#L400), [auction_ui.py:274](roly/auction_ui.py#L274)
- 클릭 후 서버의 입찰 검증·커밋, 다음 조회, Streamlit 전송, 브라우저 그리기를 거쳐 화면에 나타난다. 따라서 버튼의 접수 상태와 DB에 접수된 최고가를 별도로 보여준다. 응답을 잃으면 같은 UUID의 결과부터 확인한다. [auction_ui.py:67](roly/auction_ui.py#L67), [auction_ui.py:90](roly/auction_ui.py#L90)
- 숨긴 탭은 브라우저가 타이머를 늦출 수 있다. 복귀·포커스 시 현재 fragment에 새 조회를 요청하는 컴포넌트가 있으며, 이 신호는 입찰이나 마감 변경을 수행하지 않는다. [auction_sync.py:18](roly/auction_sync.py#L18)
- 새 배치의 완료 판단에는 다음 선수 전환, 동시 입찰과 마감, 낙찰 후 잔액, 소리 유지, 작은 화면의 포지션 목록, 같은 역할 선수의 중복 표시를 함께 확인해야 한다. 과거 화면의 리허설을 새 화면 검수로 대신하지 않는다.

기존 API 지연 계측 (`test-artifacts/auction-latency-after-remote.json`)과 Riot 최종 DB 리허설 (`test-artifacts/riot-rehearsal-final-remote.json`)은 각각 실행 당시 소스와 조건의 근거다. 후자는 가상 시계·가짜 Riot HTTP를 사용한 데이터·동시성 검증이며 브라우저 반영 속도 측정이 아니다.

## 최종 검토에서 보완한 예외

| 발견한 문제 | 수정과 확인 |
|---|---|
| 초기화 창의 첫 DB 조회 실패가 처리되지 않음 | 서비스 연결·현재 세션·경매 조회까지 오류 처리 범위에 포함했다. 안전한 안내만 표시하고 초기화나 기존 상태 변경을 실행하지 않는다. 각 진입점 실패와 민감 오류문 미노출을 UI 회귀로 확인했다. [초기화 진입](roly/auction_ui.py#L145), [회귀](tests/test_auction_reset_ui.py#L126) |
| 초기화·취소 전 입찰이 현재 기록과 섞이거나 새 효과음처럼 재생될 수 있음 | `CANCELLED` 대상 입찰을 현재 목록·소리 신호에서 제외하고 별도 접힌 기록으로 보존했다. 브라우저가 중간 준비 상태를 놓친 경우도 컴포넌트 회귀로 확인했다. [소리 신호](roly/auction_components.py#L551), [회귀](tests/test_auction_components.py#L366) |

## 구현 후 검증 범위

- 컴포넌트·Node 집중 회귀 17개 통과: 현재/다음 순서, 재경매 이력 중복 제거, 포지션 행, 이미지·숙련도·타이머·상단 독립 음량과 취소된 입찰의 효과음 제외를 검증했다. 실제 브라우저의 시각 검수와 구분한다.
- 경매 UI 집중 회귀 19개 통과(144.528초): 팀 화살표의 양 끝 순환, 다른 팀 관전 중 본인 팀 입찰, 금액 초기화의 DB·요청 번호 불변, 주최자/관전자 권한, 상단 음량을 포함한 fragment 경로 안정성을 확인했다. 정확한 10초 상한·3초 전환 검사는 DB 가상 시계와 고정된 렌더 시계를 사용하며, 렌더 지연 차감은 별도 컴포넌트 회귀로 검사한다. 실행 로그 (`test-artifacts/live-auction-ui-final-focused.log`).
- 초기화 UI 집중 회귀 7개 통과: 조회·취소의 무변경, 환불 확인, 응답 유실 재시도, 권한과 로그인 변경, 미리보기 이후 변경 거절, 다른 관전 화면의 초기화 반영과 최초 DB 조회 실패 처리를 확인했다. 준비/완료 화면 진입과 팝업 배타성은 연결 코드 검토로 구분한다. [초기화 UI 회귀](tests/test_auction_reset_ui.py).
- 체험 준비 스크립트 회귀 7개 통과: 5개 숙련도·솔로/자유 동시 반영, 실패 후 기존 캐시 보존, 429·시간 제한, 비공개 데이터 제거를 검증했다.
- 실제 체험 22명 조회: 성공 21명 모두 5개 숙련도와 솔로/자유 정보를 저장했고, 1명은 `not_found`로 남겼다. 46.703초, 85단계 처리, 임시 DB 삭제 및 실행 중 소스 해시 유지. [최신 준비 결과](test-artifacts/demo-riot-preparation.json), 직전 3개 숙련도 준비 결과 (`test-artifacts/demo-riot-preparation-top3.json`).
- TOP5·솔로/자유 계약의 DB 리허설은 PostgreSQL 15단계(74.562초), SQLite 15단계(30.641초) 통과했다. 회원·세션·점수·명단·경매의 v5→v6 보존, 429 재개, 키 회전, 느린 HTTP와 다른 팀 입찰의 동시 진행, DB 호출 한도와 비공개 권한을 확인했다. 실제 DB 연결과 가짜 Riot HTTP·가상 시계를 사용했으며 실제 Riot 요청은 0회다. 새 QA 스키마·임시 DB 삭제 및 종료 시 113개 파일 해시 불변을 확인했다. 이 결과는 뒤이어 수정한 UI의 검증을 대신하지 않는다. [PostgreSQL 결과](test-artifacts/final-ui-riot-remote.json), [SQLite 결과](test-artifacts/final-ui-riot-sqlite.json), 소스 비교 (`test-artifacts/riot-final-manifest-comparison.json`).
- 최종 전체 회귀 508개 통과(1061.085초), 실행 중 검사 대상 120개 소스 변경 0개를 확인했다. [최종 실행 보고](test-artifacts/riot-auction-verified-tests-report.json) · [전체 로그](test-artifacts/riot-auction-verified-tests.log). 실제 브라우저에서 새 배치의 CSS·스크롤·기기 음량을 확인하는 작업은 이번 자동 검증에 포함하지 않았다. 아래 항목은 회귀와 남은 실기기 확인을 구분하는 기준이다.

1. 현재 → 다음 표시가 저장된 무작위 순서와 일치하고, 유찰 재경매·낙찰 취소·마지막 선수에서 가짜 다음 대상을 만들지 않는다.
2. 남은 인원은 중복된 과거 lot 이력과 분리해 계산한다.
3. API 조회부터 공개 캐시·실제 체험 스냅샷·현재 카드까지 최대 5개 숙련도가 이어진다. 캐시가 3개라면 임의로 5개를 채우지 않는다.
4. 전체 현황은 포지션 행으로 표시하면서 같은 포지션 선수와 빈 자리, 팀장 표시, 잔액을 모두 보존한다.
5. 현재 티어는 솔로/자유랭크, 클랜 티어와 전력은 각자의 원본·확정 시점을 구분한다.
6. 전체 관전, 팀장 입찰, 주최자 운영, 관리자 정정의 기존 권한 경계를 유지한다.
7. 동결된 최종 소스로 필요한 회귀를 다시 실행하고, 새 화면의 실제 검수 여부와 미검증 범위를 따로 기록한다.
