# 동시 입찰·마감 경쟁 검수 결과

검수일: 2026-09-09. 배포 앱 rolymoly-test의 팀장 4개 세션과 관전자 1개 세션, 그리고 별도의 임시 PostgreSQL 스키마에서 확인했다. 제품 코드·랭크 스키마·운영 정책은 이번 검수에서 변경하지 않았다.

## 판정

**DB의 입찰 순서·마감·중복 방지는 통과했다. 실제 브라우저의 동일 금액 경쟁도 확인했다. 다중 세션의 화면 갱신 지연이 관찰되어 실시간 응답성 전체를 통과로 판정하지 않는다.**

같은 금액에서는 서버 트랜잭션이 먼저 유효하게 확정한 입찰이 인정된다. 브라우저에서 누른 시각이나 로컬 PC 시각으로 우선순위를 판단하지 않는다. 현재 최고 금액과 같거나 낮은 후속 요청은 인정되지 않고, 유효한 더 높은 입찰은 최고 입찰자를 변경한다.

## 실제 Cloud 브라우저

사용자 제공 테스트 회원·계정으로 독립 로그인했다. 1팀 겨울, 2팀 Kging, 3팀 슬모띵, 4팀 마아먕고로룡이며 관전자 오도봉구는 해당 경매 참가 명단 밖의 승인 회원이다. 임시 경매 3번만 사용했다. 동일 PC의 독립 브라우저 세션이므로 여러 물리적 기기·통신망을 검증한 것은 아니다.

| 조건 | 관찰·DB 대조 | 판정 |
| --- | --- | --- |
| 네 팀 동일 금액 경쟁 | 경 먀#kr1에 각 10P 제출 버튼 조작 4건 완료. DB 접수는 1팀의 1건 | 접수 중복 없음 |
| 실제 마감·연장 | 최초 마감 07:31:02.658680 UTC, 접수 07:30:59.934420 UTC. 최초 마감 2.724260초 전. 새 마감 07:31:07.658680 UTC | +5.000000초 연장 |
| 낙찰·차감 | 1팀 1000→990P, 팀원 1→2명. 나머지 세 팀 1000P/1명 유지 | 중복 차감 없음 |
| 기록·관전 | 다섯 세션 모두 같은 10P 입찰 기록 표시. 관전자에게 입찰 버튼 없음 | 확인 |
| 화면에 남은 1초 표시에서 다른 금액 경쟁 | 라라루루#kr0에 10/20/30/50P 버튼 조작 4건 완료. 추가 접수 기록 없음 | 마감 직전 수락 성공으로 집계하지 않음 |
| 주최자 일시정지 | 다섯 세션 동일 선수/남은 26초 표시, 팀장 입력·입찰 비활성화. 재개 제어는 주최자에게만 표시 | 확인 |

브라우저 클릭 완료는 서버 접수 완료와 다르다. 1초 시도의 거절 원인별 서버 응답을 보존하지 못했으므로 모두 서버 마감 검증에서 거절됐다고 단정하지 않는다. 자동화 전달 시간, 화면 갱신 지연, UI의 오래된 요청 방어 경로를 분리 계측해야 한다. 로컬 시각과 DB 시각에 차이가 있어 서로 빼서 네트워크 지연을 계산하지 않았다.

## 별도 PostgreSQL 정밀 경쟁

11개 단계 모두 통과했다. 아래는 준비·정리를 제외한 주요 결과이다. 공유 회원·경매에 요청하지 않고 임시 스키마에서 실제 DB 시계와 쓰기 잠금을 사용했다.

| 조건 | 결과 |
| --- | --- |
| 네 팀 동일 10P | 1건 접수, 3건 거절 |
| 10/20/30/40P 경쟁 | 이번 실행의 확정 순서 10→30→40, 20은 거절. 최종 40P |
| 같은 요청 번호 네 번 | 1건 접수, 3건 기존 결과 재사용. 다른 계정·금액으로 같은 번호 재사용 거절 |
| 성공 응답 유실 | 성공 결과를 호출자에게 전달하지 않은 뒤 조회·재시도해 같은 접수 반환. 중복 접수·연장 없음 |
| DB 마감 직전 네 요청 | 마감 0.802570초 전 1건 접수, 3건 거절, 정확히 +5초 |
| 마감 전 잠금 대기 네 요청 | DB 잠금 대기 4개 확인. 마감 0.126920초 후 잠금을 풀자 4건 거절, 접수 0건 |
| 낙찰 처리 네 번 경쟁 | 낙찰 이벤트·25P 차감 각 1회, 회원 전력점수 불변 |

전체 단계 29.234초. 요청별 측정값 0.140~0.516초는 해당 실행 환경의 서비스 호출 시간이며 Cloud 브라우저의 클릭→화면 응답 시간이 아니다.

## 발견한 화면 응답성 문제와 후속 검증

- 같은 시점에 일부 세션은 다음 선수의 남은 시간을 표시하고 다른 세션은 직전 선수의 0초를 표시했다.
- 증액한 입력 값이 버튼 문구에 늦게 반영되거나, 재렌더링 중 이전 버튼과 새 버튼이 DOM에 함께 존재했다.
- 입찰 값·선수·버튼이 일치하지 않는 첫 시도는 자동화가 제출하지 않았다. 실패한 준비 시도를 성공 입찰 수에 넣지 않았다.
- 읽기 검토상 폴링 1회에는 로그인 권한·경매·목록·DB 시각 SELECT 8개가 있다. 과거 유찰/취소 차례와 반복 Riot 프로필 변환, 접힌 이력 표도 반복 처리한다. 현재 현상의 단일 원인으로 확정할 처리시간 계측은 아직 없다.

후속 우선순위는 세션별 조회/렌더/입찰 응답/화면 수신 시간을 계측하고, 현재 회차와 과거 이력 조회를 분리하며, 요청 안에서 프로필 중복 변환을 줄이는 것이다. 권한 검증·서버 마감 검증은 유지해야 한다. 개선 후 다중 세션에서 입력 반영·마감 전후 경쟁·관전자 표시를 재검수해야 한다. 랭크 정규화 시에도 선수마다 추가 조회하거나 회원당 두 행을 반환해 폴링 부하를 늘려서는 안 된다.

## 검수 데이터 정리

- 임시 PostgreSQL 스키마: 삭제 후 부재 확인.
- Cloud 임시 경매 3번: 주최자 일시정지 후 정확한 ID/제목/주최자·외래키·보호 해시 검사. 10개 테이블에서 관련 111행 삭제, 커밋 후 잔여 0행 확인.
- 회원 22명, 계정 23개, Riot 프로필 20개, 기존 경매 1번 및 관련 데이터 보존.
- 새 Riot API 호출 없음. 모든 브라우저 검수 세션은 라운지로 이동.
- 증거 문서와 실행 도구는 로컬 test-artifacts에 보관했다. 제품 코드 변경이 없으므로 기존 전체 제품 테스트를 다시 실행하지 않았다.

## 근거

- [PostgreSQL 경쟁 결과](test-artifacts/cloud-bid-races-postgres.json)
- [동일 금액 브라우저 기록](test-artifacts/cloud-race-browser-first.json)
- [남은 1초 브라우저 기록](test-artifacts/cloud-race-browser-deadline.json)
- [DB 접수 시각·연장 대조](test-artifacts/cloud-race-deadline-analysis.json)
- [일시정지 후 다섯 세션](test-artifacts/cloud-race-browser-paused.json)
- [임시 경매 정리 결과](test-artifacts/cloud-race-cleanup-executed.json)
- [랭크 구조·운영 기준 검토](test-artifacts/RANK_AND_OPERATING_POLICY_REVIEW.md)
