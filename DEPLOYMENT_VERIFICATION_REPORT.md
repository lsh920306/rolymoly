# 배포 준비 검증 보고 — 2026-09-09

> 이 문서는 이전 배포 검수 이력입니다. 최신 배포 경로 수정·라운지 복구·남은 응답성 확인 상태는 [현재 구현 보고서](docs/ROLYMOLY_IMPLEMENTATION_REPORT.md)를 참고하세요.

사용자의 명시적 승인 후 첫 배포 `a3a091d`, 중간 개선본 `cf92f09`에 이어 **최신 `56f6c13`을 `main`과 `rolymoly-test`에 반영하고 Cloud 업데이트·정식 재시작을 확인했습니다.** Python 3.11.16·Streamlit 1.63 빌드, 비로그인·회원·관리자 전체 페이지와 전체 팀 팝업 표시, 최신 3단 배치·200px 원본 로고를 확인했습니다. 입찰 금액·팝업 경합 수정본의 고유 테스트 606개와 이후 화면 변경의 영향 검증 35개가 통과했습니다. 브라우저 연결 복구 후 **최신 Cloud 초기화 이후 새 10 P 직접 입찰·오른쪽 기록 표시·낙찰·팀원 추가·1,000→990 P 차감**을 확인하고 읽기 전용 PostgreSQL 조회로 대조했습니다. [전체 페이지 검수 보고](BROWSER_PAGE_REVIEW.md)와 [현재 배포 상태](DEPLOYMENT.md#현재-배포-상태)를 함께 확인합니다.

606개 전체 검증 이후 **왼쪽 팀 현황 / 가운데 경매·입찰 / 오른쪽 입찰 기록**의 3단 배치와 사용자 원본 로고 [static/rolymoly-logo.png](static/rolymoly-logo.png)의 200px 너비 표시를 추가했습니다. 이 변경의 로컬 실제 브라우저 검수와 관련 UI·컴포넌트 자동 회귀 35개 통과를 확인했습니다. 606개를 이 추가 변경 이후 전체 재실행한 것으로 표기하지 않습니다.

새 배치의 1920×855 로컬 화면에서 세 패널의 상단 일치·너비 약 346/773/310px·가로 넘침 없음을 확인했습니다. 입찰 기록은 흰 배경·진한 글씨이며, 로고는 200×67.53px로 잘림이 없습니다. 전체 팀 팝업은 720×798.70px·내부 스크롤 없음·닫기 1회 뒤 닫힘 유지였습니다. 이 확인에서는 새 입찰과 Riot 추가 조회를 하지 않았습니다. [로컬 배치·로고 검수](test-artifacts/auction-layout-logo-browser.json)

3단 배치·로고 추가 전 마지막 전체 검증은 **고유 606개 통과·542.563초·누락/중복/예상외/미완료/테스트 오류/스킵 0**입니다. 소스 140개는 시작·종료·최종 재확인 SHA-256이 일치했습니다. 4번 실행기의 진행 JSON 교체가 Windows `WinError 5`로 8개 통과 뒤 중단되어 원래 종료 코드 1과 로그를 보존했습니다. 제품 소스를 변경하지 않고 미실행 138개만 재개해 통과했고, 전체 고유 테스트 ID 대조로 결과를 확정했습니다. [606개 전체 검증](test-artifacts/deployment-interaction-final-summary.json)

3단 배치·로고 추가 후 **관련 테스트 35개가 93.640초에 모두 통과**했고 종료 코드 0, 검사 대상 소스 140개의 전후 변경 0을 확인했습니다. 606개 전체 검증과 이후 35개 영향 검증을 구분하여 보존합니다. [최종 화면 영향 검증](test-artifacts/deployment-logo-three-column-focused-report.json)

추가 시간 상한 확인에서는 기존 테스트 **9개를 다시 실행해 통과**했습니다. 유효 입찰마다 남은 시간에 5초를 더하되 해당 경매에서 선택한 5/10/15/20/25/30초를 넘지 않습니다. 실행 전체 34.063초·소스 140개 변경 없음·외부 DB/Riot 호출 0건이며, 이 확인을 위한 제품 코드 추가 변경은 없습니다. [선택 시간 상한 확인](test-artifacts/deployment-selected-bid-time-focused-report.json)

중간 개선본의 전체 자동 검증은 **599개 통과·462.953초·테스트 ID 누락/추가/중복 0**입니다. 소스 138파일 중 실행 도중 `static/app.css`만 변경되어 전체 실행의 `source_unchanged=false` 근거를 유지했습니다. 이후 최종 CSS **컴포넌트 17개를 1.172초에 재검증해 통과**했고 재검증 중 소스 변경은 없습니다. 이 이력은 위 최신 606개 검증과 구분합니다. [중간 전체 결과](test-artifacts/deployment-cloud-final-verification.json) · [당시 최종 CSS 검증](test-artifacts/deployment-cloud-final-css-recheck-report.json)

이 문서의 전체 585개·마지막 수정 영향 54개와 아래 575개 검증은 배포 전에 완료한 해당 시점 소스의 이력입니다.

배포 모드 분리, 본인 닉네임 변경, 앱 스키마 백업·복원과 경매 워커 복구를 검증했습니다. 팀장 선택·참가 팀 카드 변경 당시 **고유 테스트 575개가 모두 통과**했습니다. 전체 발견 목록과 통과한 테스트 ID를 대조해 누락 0개를 확인했고, 검사 전후 소스 135개 해시가 일치했습니다. 검수 계정 22개는 사용자의 요청에 따라 배포 후에도 재사용할 수 있도록 공유 검수 DB에 보존합니다.

## 팀 카드 수정 당시 검증

| 항목 | 결과 |
| --- | --- |
| 카드 변경 전 전체 자동 회귀 | **565개 PASS·1070.195초·종료 코드 0**, 실행기 1072.281초·소스 132개 해시 일치. [결과 파일](test-artifacts/deployment-verified-tests-report.json) · [실행 로그](test-artifacts/deployment-verified-tests.log) |
| 최종 팀장·참가 팀 카드 변경 후 전체 회귀 | **575개 전체 PASS·예상 575개·누락 0개·소스 135개 해시 일치**, 최종 집계 종료 코드 0. [최종 대조 결과](test-artifacts/deployment-final-verification.json) |
| 카드 변경 전 코드의 실제 PostgreSQL 리허설 | **5개 시나리오·72단계 PASS, 총 355.986초**. 각 실행의 소스 132개 해시 일치 |
| 앱 스키마 백업·복원 | **35개 테이블·20개 sequence 대조 PASS, 75.422초** |
| 실제 Riot 수동 조회 | **22명 중 20명 성공, 2명 `not_found`, 59.328초** |
| 실제 로컬 브라우저 | 본인 닉변·회원 22명 표시·일반내전 생성과 결과·관리자 종료, 팀장 4명 저장·팝업 외곽 520px, 최종 4팀 카드 정렬 확인 |
| 공유 검수 데이터 | 총 계정 23개·승인 회원 22명 보존. 임시 QA 스키마와 구분 |
| 원격 소스·배포 | 이 검증 이후 사용자 승인으로 첫 배포·중간 개선을 거쳐 최신 `56f6c13`의 Cloud 업데이트와 정식 재시작 확인 |
| 배포 commit·URL | 최신 연결·반영 상태는 [배포 문서](DEPLOYMENT.md#현재-배포-상태)에 통합 |

최초 4개 묶음 병렬 실행은 **566개 통과·테스트 보조 모듈 import 오류 3개·종료 코드 1**, 458.297초였습니다. 앱 오류가 아니라 개별 모듈 실행 시 `tests` 폴더가 검색 경로에 빠진 문제였습니다. 제품 소스를 변경하지 않고 자식 프로세스에 `PYTHONPATH=tests`를 지정해 누락된 3개 모듈의 **9개 테스트를 74.266초에 모두 통과**했습니다. 두 실행의 통과 메서드 ID를 전체 발견 목록과 대조하여 575개 전체 통과를 확정했습니다. [최초 병렬 결과](test-artifacts/deployment-final-parallel-report.json) · [경로 수정 후 재검증](test-artifacts/deployment-final-import-recheck-report.json)

카드 변경 전 565개와 이전 508·166·219개 등 검증은 해당 시점의 기록으로 유지합니다. 위 최종 575개 결과와 별도로 구분합니다.

## 검수·운영 모드와 로그인

`[app] environment`는 `local`, `test`, `production`을 지원합니다. 생략하면 `production`이며 잘못된 설정은 중단합니다. `test`와 `production`은 Supabase 대상만 허용하며 Workspace·체험 계정·체험 데이터 생성을 서버에서 차단합니다. `test`에는 ‘검수용’ 배지를 표시합니다.

Cloud Secrets에 Supabase 연결 정보를 비공개로 입력하고 다음 값의 적용을 완료했습니다.

```toml
[app]
environment = "test"

[riot]
allow_demo = false
```

모드 이름이 별도 DB를 자동 생성하거나 자료를 이관하지는 않습니다. 같은 연결 정보를 사용하는 앱과 브라우저는 같은 회원·경매 자료를 봅니다. 이전 체험 세션은 토큰·DB 경로·입력 폼을 정리하고, 같은 공용 DB의 정상 로그인은 유지합니다. Cloud에서 로컬 개발용 환경변수나 SQLite 경로를 사용하지 않습니다. [구현](roly/deployment_config.py) · [화면 경계](roly/ui.py) · [Cloud 설정 순서](DEPLOYMENT.md#검수-앱의-cloud-설정)

## 공유 검수 계정과 실제 Riot 조회

검수 DB에는 관리자 1개를 포함한 계정 23개와 사용자가 제공한 Riot ID의 승인 회원 22명이 있습니다. 22개 개인계정의 로그인과 본인 닉네임 변경 후 원래 ID 복귀를 확인했습니다. 이 계정들은 사용자의 배포 후 재사용 요청에 따라 의도적으로 보존합니다. 한 브라우저에서 체험 권한을 바꾸는 방식과 달리 실제 개인계정으로 접속합니다. [검수 데이터 준비 결과](test-artifacts/deployment-shared-fixture.json)

로그인 정보는 로컬 비공개 파일 `.data/deployment-test-accounts.md`, `.data/deployment-test-accounts.csv`에만 보관합니다. **이 문서에는 비밀번호를 기재하지 않으며, 두 파일과 `.data/`는 GitHub·소스 ZIP·공개 문서에 올리지 않습니다.** DB 비밀번호·Riot API 키·PUUID도 공개하지 않습니다.

실제 Riot 키로 22명에 대한 수동 갱신을 실행하여 **20명 성공·2명 조회 불가**로 완료했습니다. 성공한 응답의 솔로·자유랭크 티어·LP·승패·판수·승률과 숙련도 상위 5개를 DB 프로필에 저장했습니다. 없는 기록은 만들어 채우지 않으며, Riot 갱신 전후 주·부 포지션·클랜 티어·점수·업적·계정은 유지됐습니다. 실행 시간은 59.328초이며 해당 조회에서 측정한 공통 요청 한도도 충족했습니다. [실제 조회 결과](test-artifacts/deployment-live-riot-22.json)

| Riot ID | 응답 | 후속 확인 |
| --- | --- | --- |
| 화려한솔로#외로운청년 | `not_found` | 사용자가 현재 게임 내 닉네임·태그를 확인한 뒤 수정·재조회 |
| 야수#테토 | `not_found` | 사용자가 현재 게임 내 닉네임·태그를 확인한 뒤 수정·재조회 |

조회 불가는 해당 시점 API 응답이며 계정 삭제나 영구적인 미존재로 단정하지 않습니다. 이 두 회원도 가입 계정과 기존 기록은 유지합니다.

## 본인 닉네임 변경

승인된 개인계정은 **본인 프로필 또는 내 계정 → 닉네임 변경**에서 `닉네임#태그`를 수정합니다. 다른 회원에게는 버튼을 표시하지 않고, 팝업에서도 현재 계정·회원·DB·수정 버전을 다시 확인합니다. 재가입하지 않으며 아래 항목을 유지합니다.

- 로그인 아이디, 계정·회원 ID 연결, 현재 로그인 세션
- 주·부 포지션, 클랜 티어, 기본점수·증감 원장·업적
- 기존 일반내전·경매 참가 연결과 당시 이름·티어 스냅샷, 팀장 입찰 권한

중복 Riot ID와 오래된 폼은 저장을 거절하고, 같은 표기를 그대로 저장하면 변경하지 않습니다. 다른 Riot ID로 바뀌면 이전 현재 티어·Riot 캐시·조회 작업을 비워 다른 사람의 API 정보가 남지 않게 합니다. 대소문자만 바뀐 동일 ID의 캐시는 유지합니다. 저장 자체로 API를 호출하지 않으며 **갱신하기**로 새 정보를 조회합니다. [서버 구현](roly/core.py) · [본인 편집 팝업](roly/self_profile_ui.py) · [백엔드 검증](tests/test_self_profile.py) · [화면 검증](tests/test_self_profile_ui.py)

## 실제 PostgreSQL 리허설 72단계

아래 다섯 실행은 카드 화면을 추가 변경하기 전 기준 소스에서 각각 새 QA 스키마를 생성하여 수행했습니다. 모든 단계가 통과했고, 실행 전후 동결한 소스 132개의 해시가 같으며, 생성한 QA 스키마의 삭제와 부재를 확인했습니다. 공유 검수 회원을 이 리허설의 합성 자료로 덮어쓰지 않았고 실제 Riot HTTP도 호출하지 않았습니다.

| 리허설 | 단계 | 시간 | 주요 확인 | 근거 |
| --- | ---: | ---: | --- | --- |
| 경매 워커 복구 | 9 | 66.032초 | 워커 강제 종료·새 프로세스 복구·다음 선수 3초·일시정지 유지·두 워커 중복 정산 방지 | [결과](test-artifacts/deployment-recovery-remote.json) |
| 회원·일반내전 | 19 | 79.969초 | 가입·권한·점수·정정·본인 닉변·동시 변경·진행 중 Riot 응답의 이전 ID 반영 차단 | [결과](test-artifacts/deployment-member-normal-remote.json) |
| 경매·대진·업적 | 17 | 66.875초 | 경쟁 입찰·UUID 재전송·시간 연장·정산·팀 구성·결과와 업적 | [결과](test-artifacts/deployment-competition-remote.json) |
| Riot 동기화 | 15 | 64.672초 | 모의 HTTP로 요청 한도·429 재개·키 교체·입찰과 독립적인 HTTP·API 캐시 분리 | [결과](test-artifacts/deployment-riot-remote.json) |
| 경매 초기화 | 12 | 78.438초 | 권한·버전 검사·환불·배정 초기화·기존 기록 보존·중복 요청·입찰 경합 | [결과](test-artifacts/deployment-auction-reset-remote.json) |
| 합계 | **72** | **355.986초** | 모든 임시 QA 정리 완료 | |

## 백업과 복구의 완료 범위

[백업 도구](scripts/backup_storage.py)는 PostgreSQL 17의 `pg_dump`·`pg_restore`를 사용하여 비공개 앱 스키마 전체를 저장하고, 새 QA 스키마에 복원해 원본과 비교합니다. 실제 Supabase의 합성 자료로 선행 서비스 흐름 15개를 수행한 뒤 **35개 테이블·20개 sequence의 행·구조 대조를 75.422초에 통과**했습니다. 원본 QA와 복원 QA, 합성 백업은 정리했습니다. [백업·복원 결과](test-artifacts/deployment-backup-rehearsal.json)

계정·세션·회원·원장·입찰·대진·스냅샷을 포함하는 앱 스키마 검증입니다. 운영 스키마를 덮어써 복원한 결과가 아니며, Supabase 프로젝트의 역할·설정·확장·외부 자산은 포함하지 않습니다. 예약 백업·외부 보관·실패 알림·실제 운영 복원 절차는 별도로 마련해야 합니다. 원본 백업 `.data/backups/`는 민감 자료이며 공개 업로드 대상에서 제외합니다. [명령과 범위](DEPLOYMENT.md#백업과-복구-범위)

## Cloud 지연 개선 중 완료한 PostgreSQL 검증

Cloud 화면 반영 지연을 확인한 후 PostgreSQL 연결풀과 중복 조회 제거를 반영했습니다. 이 중 연결풀과 핵심 서비스의 실제 PG 검증은 아래와 같이 완료했습니다. 각 검증은 새 임시 QA 스키마를 사용했고 해당 스키마를 정리했으며, 실제 Riot API를 호출하지 않았습니다.

| 검증 | 완료 결과 | 근거 |
| --- | --- | --- |
| 연결풀·격리 | **13조건 통과**. 동시 연결 제한, 반납 후 물리 연결 재사용, 스키마·읽기 전용 설정 누출 차단, 임시 객체 정리와 끊어진 연결 복구 확인. 생성한 두 QA 스키마 정리 완료 | [결과](test-artifacts/deployment-postgres-pool.json) |
| 인증·경매·일반내전 | **17단계 통과, 48.703초**. 동일가 입찰 4건 중 1건 수락, 두 워커의 1회 정산·차감, +5초 연장·상한·마감 경계·다음 선수 3초, 16명 낙찰·총 170 P 차감, 일반내전 점수·정정과 경매 보상 확인 | [결과](test-artifacts/cloud-pool-domain-postgres.json) |

핵심 서비스 리허설의 실행 전후 소스 138파일과 공용 회원·기존 이벤트 1번의 해시가 같고, 임시 스키마의 실제 부재도 확인했습니다. Cloud 검수용 이벤트 2번에는 읽기 전용 상태 조회만 수행했습니다. 이 PG 검증과 위 전체 자동 테스트의 완료는 재배포 후 Cloud 브라우저 입찰 성공을 대신하지 않습니다.

## 실제 브라우저 확인

아래 표의 카드 크기와 경기 저장은 먼저 완료한 로컬 실제 브라우저 검수 이력입니다. 이후 `a3a091d`의 Cloud 빌드와 비로그인·회원·관리자 전체 페이지를 별도로 확인했으며, Cloud 경매의 추가 검수 상태는 아래 배포 절에 구분합니다.

| 흐름 | 확인 상태 |
| --- | --- |
| 본인 닉네임 변경 | 저장 후 바뀐 이름 표시, 동일 계정 로그인 유지 확인 |
| 라운지·회원 목록 | 회원 22명 표시와 주 포지션·부 포지션 별도 열 확인 |
| 일반 회원의 일반내전 | 명단 10명 정확 일치 → 균형 팀 편성 → 결과 저장·점수 반영 → 관리자 종료 확인 |
| 경매 팀장 지정 팝업 | 실제 팀장 4명 저장과 팝업 외곽 너비 520px 확인 완료 |
| 최종 참가 팀 카드 | 서버 재시작 후 4팀 모두 사진·티어·주부 포지션·첫 숙련도 아이콘의 왼쪽 기준 일치, 숙련도 5개 한 줄, 빈자리 안내·하단 포지션 행 제거를 실제 DOM·스크린샷으로 확인 |
| 긴 Riot ID 표시 | `화려한솔로#외로운청년`을 실제 팀장 카드에 표시해 카드 너비 279px·텍스트 너비 110.91px·높이 18px·넘침 없음 확인. 말줄임·줄바꿈 없이 한 줄 표시 |
| 실제 Cloud 페이지 | 최신 `56f6c13` 반영·정식 재시작 확인. Python 3.11.16·Streamlit 1.63과 전체 페이지/권한 동선, 720px·제목 26px·4팀 팝업, 최신 3단 배치·200px 로고 확인 |
| 최신 로컬 조작·공유 PostgreSQL | 팝업 닫기 1회 뒤 닫힘 유지 → 경매 재개 → 10 P 입찰 → 낙찰 → 1,000→990 P 차감 확인. 입찰·낙찰 각 1건과 선수 배정을 읽기 전용 DB 조회로 대조 |
| Cloud 동기화·남은 검수 | 다른 Cloud 접속의 낙찰 동기화·재시작 직후 기존 일시정지/990 P 보존과 초기화 이후 새 Cloud 직접 입찰·낙찰·차감을 각각 확인. 여러 실제 기기의 인터넷 지연·부하 검수는 별도 |

참가 팀은 데스크톱에서 2열 흰 카드를 유지하며, 패널이 좁다는 이유로 열 수를 줄이지 않습니다. 기존 카드·선수 안쪽 여백은 각각 14px·10px로 유지하고, 왼쪽 참가 팀과 오른쪽 준비 영역의 폭 비율만 1.35:1에서 1.25:1로 소폭 조정했습니다. 각 카드 상단에는 팀 이름·인원·포인트가 있고, 선수 사진과 이름·Riot ID를 한 행에 둡니다. Riot ID는 한 줄로 표시하고 전체 ID를 툴팁에도 제공합니다.

그 아래 솔로·클랜 티어, 주·부 포지션, 실제 저장된 숙련도 상위 5개를 사진의 왼쪽 끝에 맞춰 카드 전체 폭으로 배치합니다. 숙련도 아이콘은 한 줄에 놓고, 빈자리 안내와 하단의 별도 5포지션 행은 제거했습니다. 선수별 주·부 포지션과 기존 모바일 1열 배치는 유지합니다. 카드 전용 테스트 7개와 마지막 간격 조정을 포함한 최종 575개 전체 회귀가 통과했습니다. [카드 구현](roly/team_cards.py) · [전용 검증](tests/test_preparation_team_cards.py)

## 배포 반영과 남은 확인

사용자가 `main` 반영을 명시적으로 승인하여 첫 배포 `a3a091d`, 중간 개선본 `cf92f09`에 이어 최신 `56f6c13`을 `main`과 `rolymoly-test`에 반영했습니다. 2026-09-09 06:17:44 UTC Cloud 업데이트와 정식 재시작을 확인했습니다. Cloud는 `main`의 [streamlit_app.py](streamlit_app.py)를 시작점으로 사용하고, **Python 3.11.16·Streamlit 1.63**의 실제 빌드를 확인했습니다. 현재 진입점은 [app.py](app.py)와 같은 앱을 실행하며, 이후 재배포도 Python 3.11과 [requirements.txt](requirements.txt)를 유지합니다.

Cloud Secrets의 Supabase 연결, `[app] environment = "test"`, `[riot] allow_demo = false`를 적용했고 비로그인·개인회원·관리자의 페이지와 로그인/권한 동선을 확인했습니다. 공유 DB의 회원 22명·계정 23개·Riot 캐시 20개·기존 경매 1번을 보존했습니다. [배포 상태](DEPLOYMENT.md#현재-배포-상태)

별도 임시 이벤트 2번에서 Cloud 경매를 검수했습니다. 연결풀·중복 조회 제거와 전체 팀 팝업 개선에 더해 입찰 금액 유지·팝업 경합 수정, 3단 배치·원본 로고가 최신 `56f6c13`에 포함됐습니다. Cloud에서 720px 너비·26px 제목·2×2의 4팀을 스크롤 없이 확인했고 최신 3단 배치와 200px 로고도 확인했습니다.

최신 로컬 브라우저에서 **팝업 닫기 1회 뒤 닫힘 유지 → 경매 재개 → 10 P 입찰 접수 → 낙찰 → 1,000→990 P 차감**을 확인했고, 다른 Cloud 접속에도 결과가 동기화됐습니다. 공유 DB 읽기 전용 조회에서 입찰·낙찰 각 1건, 선수 배정과 포인트 차감을 확인했으며 회원 22명·계정 23개·Riot 프로필 20개와 기존 경매 1번의 보호 해시는 일치했습니다. 브라우저 관찰 시각과 DB 시각을 실제 전송 시각에 맞춰 측정하지 않았으므로 응답 지연 수치로 사용하지 않습니다. [실제 입찰 DB 대조](test-artifacts/browser-live-bid-postgres-20260909.json)

최신 배포의 **초기화 전 정식 재시작 직후** 읽기 전용 DB 확인에서 일시정지·남은 약 8.919초, 기존 10 P 입찰·낙찰 각 1건과 남은 포인트 990 P가 보존됐습니다. 회원 22명·계정 23개·Riot 프로필 20개와 기존 경매 1번의 보호 해시 8개도 일치했습니다. 이 결과는 이후 초기화 뒤의 새 Cloud 입찰 성공을 뜻하지 않습니다. [재시작 직후 DB 확인](test-artifacts/cloud-event2-reboot-dry-run.json)

**입찰 금액·팝업 수정본의 고유 606개 검증과 이후 3단 배치·로고 영향 검증 35개를 완료했고 `56f6c13`으로 배포했습니다. 초기화 이후 새 Cloud 직접 10 P 입찰·오른쪽 기록 표시·낙찰·팀원 추가·1,000→990 P 차감까지 확인했습니다.** 브라우저 도구 시간초과로 중단된 검수는 브라우저 재시작 후 성공했습니다. 07:01:16 UTC의 읽기 전용 보고서에서 새 입찰 ID 2·선수 차례 75의 낙찰 및 보호 해시 일치를 대조했습니다. 이전 로컬 입찰 ID 1은 초기화로 취소된 차례의 이력이며 새 입찰과 구분합니다. [보존한 새 Cloud 입찰 DB 확인](test-artifacts/cloud-event2-cloud-bid-success-preserved.json)

최종 Cloud 1920×855 화면에서 전체 팀 팝업 720×798.70px·4팀·내부 스크롤 없음, 갱신된 990 P·2/5명 표시와 닫기 1회 뒤 반복 갱신에도 닫힘 유지를 확인했습니다. 3개의 흰 패널은 상단 일치·너비 약 346/773/310px·가로 넘침 없음이었고, 원본 로고는 200×67.53px로 잘리지 않았습니다. [최종 Cloud 브라우저 확인](test-artifacts/cloud-browser-final-56f6c13.json)

브라우저의 남은 25초·입력 0 P → 남은 22초·입력 10 P → 제출 관찰은 요청 전송과 응답을 계측한 네트워크 지연 수치가 아닙니다. 여러 실제 기기의 마지막 1초 경쟁·부하 검증은 별도입니다.

사용자의 명시적 승인 후 Cloud에서 임시 이벤트 2번을 일시정지하고 대상·외래키·보호 해시를 확인해 **해당 이벤트와 하위 검수 기록만 정리했습니다.** 1.344초에 완료했으며 커밋 후 대상 10개 테이블의 해당 이벤트 행은 모두 0개입니다. 회원 22명·계정 23개·Riot 프로필 20개·기존 경매 1번의 보호 해시 8개와 추가 보호 해시는 동일합니다. 새 Cloud 입찰 성공 근거는 삭제 전에 별도 파일로 보존했습니다. 정리 후 실제 Cloud 라운지에서도 회원 22명·기존 경매 1개 유지, 임시 경매 부재와 오류 없는 렌더를 확인했습니다. [정리 결과](test-artifacts/cloud-event2-cleanup-executed.json)

새로 전달받은 검토 Markdown과 추가 4개 항목은 읽기 전용 검토만 수행했습니다. 비밀번호 방식·결과 승인·DB 정책은 이번 작업에서 변경하지 않았습니다. 대회 종료 후 운영진 승인 시 점수·업적을 공식 반영하는 흐름은 별도 제안이며, 현재의 즉시 반영 로직에 구현된 기능으로 간주하지 않습니다. 이번 화면 변경에서 추가 Riot API 호출은 0건이고 회원 22명·계정 23개·저장된 Riot 프로필 20개를 보존합니다.

현재 마감 처리는 Python 워커에 의존합니다. 워커가 모두 꺼지면 정산은 멈추고 재시작 후 저장된 마감 상태를 복구합니다. 이번 프로세스 리허설 통과가 **Cloud 휴면 중 독립 마감, 여러 기기의 실제 인터넷 지연, 정기 백업·운영 복원 완료**를 의미하지는 않습니다.
