# 전체 페이지 검수 — 2026-09-09

사용자가 `main` 배포를 명시적으로 승인한 뒤 `a3a091d`를 반영했고, `rolymoly-test.streamlit.app`에서 Python 3.11.16·Streamlit 1.63 빌드와 비로그인·회원·관리자의 전체 페이지를 확인했다. Cloud 경매의 화면 반영 지연은 별도 임시 이벤트 2번으로 검수 중 발견했다. 연결풀·중복 조회 제거 수정본의 전체 599개와 최종 CSS 영향 17개 자동 검증은 통과했으며, 새 커밋 재배포 후 Cloud 실입찰 재검증이 남아 있다.

아래 상세 표는 먼저 완료한 실제 로컬 브라우저의 페이지·저장·완료 기록 검수이다. 당시 공유 Supabase의 기존 회원 22명과 준비 경매는 유지하고 쓰기 검수에는 격리 SQLite 복사본을 사용했다. 이후 Cloud에서도 비로그인·회원·관리자 페이지와 권한 동선을 확인했지만, 로컬 입찰 성공을 Cloud 입찰 속도 검증으로 대신하지 않는다. 브라우저의 모든 입력 조합이나 여러 기기 인터넷 지연을 확인했다는 의미는 아니다.

## 발견하여 수정한 사항

| 문제 | 수정과 검증 |
| --- | --- |
| 일반내전 선수 교환을 두 화면에서 제출하면 원래 팀으로 되돌아갈 수 있음 | 명단 버전과 요청 번호를 함께 검증한다. 동일 요청은 한 번만 교환하고, 응답 유실 시 처리 결과를 다시 확인한다. SQLite 테스트 및 실제 PostgreSQL의 서로 다른 요청/동일 요청 경쟁을 검증했다. |
| 통합 프로필에서 기존 팀·낙찰 이력이 빠짐 | 경기 결과 아래 팀·낙찰 이력을 복원했다. 당시 이름·티어·포지션·전력과 낙찰 포인트를 표시하며, 미배정·팀장·일반내전의 포인트는 `—`로 표시한다. 실제 0 P 낙찰과 구분한다. |
| 최신 회원 정보를 불러오면 수정 팝업이 닫힘 | 팝업 안에서 최신 버전을 반영하여 편집을 이어간다. 오래된 값으로 덮어쓰는 것은 계속 차단한다. |
| 경매 초기화 뒤 이전 오류 문구가 남음 | 초기화가 성공하면 해당 경매의 이전 오류 알림을 제거한다. 다른 경매의 알림은 보존한다. |
| 회원 검색에 복사한 ID의 앞뒤 공백이 포함되면 검색 실패 | 앞뒤 공백을 제거하여 라운지와 검색 기준을 맞췄다. `화려한솔로#외로운청년 `으로 1명 검색되는 것을 확인했다. |
| 경기 변경 기록에 초기화·정정의 내부 JSON이 그대로 표시됨 | 사유·이동·환불 등 사용자용 한글 요약을 표시한다. DB의 원본 감사 기록은 보존한다. |
| 관리자 메뉴가 일반 회원·비로그인 사용자에게도 표시됨 | 관리자에게만 메뉴를 표시한다. 직접 주소로 접근할 때의 서버 권한 검사도 유지한다. |
| 회원 설명이 첫 화면을 과도하게 차지하고 프로필 일부 배경이 불명확함 | 점수·업적 기준을 접는 안내로 정리하고, 프로필의 숙련도·경기 기록에 흰 배경을 적용했다. |
| 전체 팀 팝업의 글씨와 팀 구분이 작고 흐림 | 최신 수정 소스에 720px 기준 2×2 배치, 큰 글씨, 보라색 팀 헤더, 녹색 잔여 포인트, 회색 빈자리와 청색 배정 표시를 적용했다. 개선본의 Cloud 표시 확인은 재배포 후 수행한다. |
| Cloud 경매 조작 뒤 화면 반영 지연 | PostgreSQL 연결 재사용과 중복 상태 조회 제거를 반영했다. 실제 PG와 전체 자동 테스트·최종 CSS 영향 검증은 완료했다. 재배포 후 Cloud 입찰·재개 검수는 남아 있다. |

## 배포 전 실제 로컬 브라우저에서 확인한 페이지

| 페이지 | 확인 내용 |
| --- | --- |
| 라운지 | 22명 회원, 저장된 활동/경매 일정, 최근 일반내전·경매 결과, 회원 카드 → 프로필 연결. 비로그인 생성 차단. |
| 회원 | 검색·포지션 필터, 주/부 포지션의 별도 열, 회원별 프로필 버튼, 관리자만 수정 버튼 표시, 두 버튼의 가로 배치. |
| 회원 프로필 | 실제 저장된 솔로/자유랭크 판수·승률과 숙련도 5개, 조회 실패 회원의 미입력 상태, 돌아가기, 팀·낙찰 이력 복원. |
| 가입 신청·내 계정 | 가입 입력과 승인 안내, 카카오톡 참가 신청 구분, 로그인·로그아웃, 본인 닉네임 변경 창, 비밀번호 변경·복구 항목. 기존 회원은 재가입 없이 본인 계정 유지. |
| 일반내전 | 일반 회원의 개설 화면, 10/20명 선택, 카카오톡 명단 입력, 포지션별 정원, 전력 균형 옵션. 비로그인 개설 차단. |
| 경매 준비 | 주최자인 일반 회원의 팀장/경매 설정 권한, 5초 간격의 5~30초 선택, 팀별 포인트, 참가 팀 2열과 5개 숙련도 아이콘. |
| 실시간 경매 | 상단 현재·다음 선수 화살표, 왼쪽 팀 전환·전체 팀 팝업, 일시 정지·재개, 팀장 입찰, 상단 음량·초기화, 유찰 발생과 재시작 활성 조건. 네 팀 팝업은 검수 화면에서 스크롤 없이 표시됐다. |
| 경기 기록 | 일반내전/경매 탭의 결과·명단·전체 경기 이력, 완료 일반내전의 1/1 경기와 우승팀, 완료 경매의 3/3 경기·결승·우승팀, 당시 팀 명단과 포인트. |
| 운영 관리 | 가입 승인, 회원·점수, 점수 정책, 업적 관리, 운영 계정, 변경 기록의 6개 탭. 일반 회원·비로그인 직접 접근 차단. |

격리 브라우저에서 팀장 `roly_test_01`의 **100 P 입찰 → 낙찰 → 팀원 1명 추가 → 1,000 P에서 900 P로 감소**를 확인했다. 일시 정지 중에는 입찰 입력이 비활성화되고 남은 시간이 보존됐다. 음량은 키보드로 80%에서 75%로 변경됐다. 관리자 클랜 티어 수정은 회원 목록과 프로필에 반영되며 과거 경기 명단은 당시 값을 유지했다.

짧은 입찰 구간에서 브라우저 자동화의 클릭 사이에 마감된 시도가 있었다. 이는 서버 도착 시각을 측정한 인터넷 지연 수치가 아니므로 실제 사용자 입찰 속도의 보장값으로 사용하지 않는다. 충분한 시간을 둔 재검증에서는 입찰·낙찰 연결이 확인됐다.

## 자동 검증과 증빙

- 배포 전 전체 unittest: **585개 통과**, 실패·오류·스킵 0. 발견한 테스트 ID와 실행한 ID가 일치한다. `test-artifacts/browser-full-suite-report.json`.
- 전체 실행 도중 추가한 초기화 알림 수정 2파일은 **7모듈 54개 영향 테스트가 모두 통과**했다. 영향 검증 전후 136파일의 해시가 같고, 테스트 ID 누락·추가가 없다. 최종 소스 해시와 결과는 `test-artifacts/browser-final-verification.json`에 기록했다.
- 실제 PostgreSQL 선수 교환: 다른 요청 경쟁은 저장 1/오래된 요청 차단 1, 같은 요청 경쟁은 저장 1/기존 결과 반환 1. 임시 스키마 삭제 확인. `test-artifacts/browser-swap-postgres.json`.
- 기존 Riot 실조회 결과는 20명 성공/2명 계정 미발견이다. `화려한솔로#외로운청년`, `야수#테토`의 최신 ID는 사용자 확인 대기이며 임의 현재 티어를 넣지 않았다. 이번 페이지 검수에서는 추가 Riot API 조회를 하지 않았다.
- Cloud 지연 개선 중 연결풀 검증: 새 임시 PostgreSQL 스키마에서 **13조건 통과**. 연결 상한·반납 후 재사용·스키마/읽기 전용 설정 격리·연결 단절 복구를 확인하고 임시 스키마를 정리했다. `test-artifacts/deployment-postgres-pool.json`.
- 연결풀 변경 후 핵심 서비스 검증: 별도의 새 PostgreSQL QA 스키마에서 **17단계 통과, 48.703초**. 동시 입찰 4건 중 1건 수락, 두 워커의 1회 정산, 입찰 연장·마감 경계·다음 선수 3초, 일반내전 점수·정정과 경매 보상을 확인했다. 소스 138파일 해시와 공용 회원·이벤트 1번은 전후 같고, 임시 스키마 삭제·부재를 확인했다. `test-artifacts/cloud-pool-domain-postgres.json`.
- 연결풀·중복 조회 제거 수정본의 **전체 599개가 462.953초에 통과**했다. 발견/실행 테스트 ID의 누락·추가·중복은 없다. 소스 138파일 중 실행 도중 `static/app.css`만 변경되어 전체 보고서의 `source_unchanged=false`를 보존했다. `test-artifacts/deployment-cloud-final-verification.json`.
- 최종 CSS의 **컴포넌트 17개가 1.172초에 통과**했고 재검증 중 소스 변경은 없었다. 전체 실행과 최종 CSS 영향 검증을 분리하며 599개 전체를 마지막 CSS 변경 이후 다시 실행한 것으로 집계하지 않는다. `test-artifacts/deployment-cloud-final-css-recheck-report.json`.

## 데이터와 배포

회원 22명·관리자 포함 23개 검수 계정·저장된 Riot 프로필 20개는 Supabase에 유지한다. 비밀번호는 비공개 `.data/deployment-test-accounts.csv`/`.md`에만 두고 공개 소스에 포함하지 않는다. 로컬 브라우저 검수용 SQLite·예시 경기·수정 이력과 해당 격리 서버는 점검 후 삭제·종료했다. 로컬 마지막 초기화 검수에서는 100 P 반환, 네 팀 모두 1/5명·1,000 P, 시작 준비 상태 복귀를 확인했다.

Cloud는 승인된 `main`의 `a3a091d`를 실행했고, Secrets의 `[app] environment = "test"`와 `[riot] allow_demo = false`를 적용했다. 비로그인·개인회원·관리자로 전체 페이지와 로그인/권한 동선을 확인했다. [현재 배포 상태](DEPLOYMENT.md#현재-배포-상태)

기존 이벤트 1번을 보존하고, Cloud 실입찰 검수용 **이벤트 2번 `Cloud 배포 검수 af178a12`**를 공식 서비스로 별도 생성했다. 회원 1~20의 참가 포지션은 기존 명단에서 복사했고 팀장 1·2·3·4, 각 1,000 P, 입찰 30초로 준비했다. Cloud의 시작·일시정지·재개 동선에서 화면 반영 지연을 발견했다. 읽기 전용 진단에서는 DB의 일시정지가 유지된 구간과 장기 advisory 잠금 대기가 관측되지 않은 표본을 기록했다. 이는 지연 원인이 모두 해결됐다는 검증은 아니다. [공유 DB 기준선](test-artifacts/cloud-deployment-db-baseline.json) · [임시 경매](test-artifacts/cloud-deployment-auction-fixture.json) · [지연 진단](test-artifacts/cloud-auction-stall-readonly.json)

자동 검증 이후 추가 로컬 입찰을 시도했지만 클릭 도구 왕복 중 다음 선수로 넘어가 입찰 기록을 아직 확인하지 못했다. 이 시도는 성공으로 집계하지 않고, 위 배포 전 격리 브라우저의 100 P 낙찰 이력과 구분한다.

남은 순서는 새 커밋 재배포 → Cloud 팀장 입찰·관전자 반영·일시정지/재개·초기화 재검증이다. 임시 이벤트 2번은 검수 동안 보존하고, 별도 지시 후 해당 이벤트만 정리한다. 여러 실제 기기의 마지막 1초 경쟁 입찰, 무료 호스팅 휴면 중 마감, 정기 백업 운영은 별도 확인 항목이다.
