**DB 통합·경매 추가 개선 보고 — 2026-09-08**

> 과거 원자료·일회성 스크립트는 사용자의 정리 요청으로 삭제되었습니다. 인라인 경로는 과거 이력 표기이며, 최신 근거는 [Riot 연동 보고](RIOT_API_REPORT.md)에서 확인합니다.

> 이 문서는 Riot·프로필·새 경매 화면을 추가하기 전 DB 개선판의 검증 이력입니다. 현재 구현·검증은 [최신 통합 보고](RIOT_API_REPORT.md), 화면 대응은 [경매 화면 매핑](AUCTION_REFERENCE_MAPPING.md)을 기준으로 확인하세요.

이전 검토에서 재현한 6개 결함과 최종 검토에서 추가로 발견한 대화상자의 세션·권한 검증 문제를 수정했습니다. 회원 원본은 DB에서 관리하며, 일반내전·경매·기록은 같은 회원 ID로 연결합니다. 확정한 명단과 경기에는 당시 이름·티어·포지션·점수를 보존합니다. Google Sheets 연결이나 기존 회원 가져오기는 추가하지 않았습니다.

**재현 결함의 수정 결과**

| 번호 | 수정한 문제 | 적용한 처리 |
| --- | --- | --- |
| R1 | 입찰 저장 후 응답만 끊기면 다음 금액 입찰이 막힘 | 미확인 요청의 UUID·금액·선수·계정을 고정해 접수 여부부터 자동 확인. 쓰기 완료를 기다린 뒤 영수증 또는 미접수를 판단하고 새 입찰로 전환. 확인 중에는 입찰 비활성화 |
| R2 | 이전 화면에서 보지 않은 새 참가 명단을 확정 | 화면에서 확인한 명단 버전을 저장하고 확정 트랜잭션에서 비교. 바뀌었으면 최신 명단 확인 후 재확정 |
| R3 | 가입 거절 후 탈퇴 처리된 신청자의 로그인·재신청·복귀가 모두 막힘 | 최초 승인과 복귀 분리. 승인 이력이 있는 회원은 승인 상태로 복귀, 미승인 신청자는 보완·재신청 가능한 대기로 복귀 |
| R4 | 이전 회원 연결의 복구 코드와 표시 이름이 남음 | 관리자 화면의 별도 회원계정 발급·재연결 제거. 남은 기존 연결 API도 세션·복구 코드 폐기 및 표시 이름 동기화 |
| R5 | 생성 저장 후 응답 유실·동시 클릭으로 일반내전·경매가 중복 생성 | 생성 UUID·요청 내용·계정을 DB 영수증으로 묶음. 같은 제출의 재시도는 기존 내전 반환. 새 작성에는 새 UUID 사용 |
| R6 | 클랜 소개 동시 편집에서 내 입력 유실 후 잘못된 성공 표시 | 편집 시작 값·버전 고정. 저장 충돌 시 입력을 보존하고 최신 내용 불러오기 제공 |
| R7 | 창을 열어둔 뒤 로그인 만료·계정 변경 시 생성 오류, 관리자 권한 회수 뒤 회원 수정 창에서 내부 메모 조회 가능 | 생성·명단·회원 수정 창이 다시 실행될 때 현재 세션과 권한을 검사. 이전 계정의 제출·타 주최자의 명단 편집·회수된 관리자의 내부 메모 조회 차단 |

수정 전 재현 기록은 [이전 검토](DB_INTEGRATION_REVIEW.md)와 `test-artifacts/review-*` 증거 파일에 보존했습니다. 해당 기록의 실패 결과와 과거 38개 테스트 수치는 수정 전 기준입니다.

**회원·DB 연결 개선**

- 신청 메시지는 `application_notes`, 운영진 내부 메모는 `notes`로 분리했습니다. 기존 메모를 신청자 공개 필드로 옮기지 않습니다. 본인 조회에서 내부 메모를 제외합니다.
- 승인된 개인계정에 진행자·관리자 권한을 부여합니다. 회원 연결은 고정하고 팀장 여부는 경매마다 판단합니다. 탈퇴 시 운영 역할이 있어도 접근을 차단하고 세션·복구 코드를 폐기합니다. 마지막 유효 관리자는 제거할 수 없습니다.
- 탈퇴 처리 전에 진행 중인 일반내전·경매·팀장 연결을 보여줍니다. 오래된 회원 상태로 탈퇴·복귀를 제출하면 재확인을 요구합니다.
- Supabase 설정이 없거나 잘못된 운영 공간은 연결을 중단합니다. 체험 선택과 명시적인 개발·테스트 SQLite 설정은 별개로 유지합니다.
- 경매 갱신은 인증 상태와 화면 자료를 한 DB 읽기 스냅샷에서 가져옵니다. 4팀 SQLite 계측은 연결 3회·SELECT 17회에서 연결 1회·SELECT 7회로 줄었습니다. PostgreSQL은 DB 시각 조회가 추가되며 실제 인터넷 지연을 보장하는 수치는 아닙니다.
- 참가자 표·경기 상세·CSV에 저장된 당시 클랜 티어·현재 티어·LP를 표시합니다. 티어 변경이 이미 확정한 경기 기록을 덮어쓰지 않습니다.
- PostgreSQL 스키마 v5는 신청 메시지 필드와 생성 영수증 테이블을 추가합니다. 계정·회원·점수·기록을 새로 가져오거나 초기화하는 작업이 아닙니다.

**경매 제어와 화면**

- 경매 상단에 주최자·관리자의 `일시 정지` / `경매 재개`, `유찰 재시작`을 배치했습니다. 팀장 계정이어도 해당 경매의 주최자·관리자가 아니면 진행 제어 권한은 없습니다.
- 경매를 만든 승인 회원이 그 경매의 주최자입니다. 상단에 실제 주최자 이름과 `내 경매`·`관리자 지원`·`진행 조회`를 구분해 표시합니다. 주최자는 본인 경매의 일시정지·재개·유찰 재시작을 직접 실행하고 관리자는 전체 경매를 지원합니다.
- 일시정지 중 입찰과 자동 진행은 멈추며 재개 시 남은 시간을 유지합니다. 마감 시각에 접수된 정지는 먼저 마감 여부를 판정해 늦은 입찰을 되살리지 않습니다.
- 이번 순서의 모든 선수를 진행한 뒤 유찰된 미배정 선수 전원만 무작위로 재경매합니다. 낙찰 선수 제외, 준비 3초, 동시 재시작 중복 차단을 적용합니다. 라운드 종료 후 일시정지 상태에서도 유찰 재시작이 가능합니다. 재차 유찰되면 같은 절차를 반복하며, 전원 배정되면 경매 완료·대진 준비로 넘어갑니다.
- 초기 입찰 시간 5·10·15·20·25·30초, 유효 입찰마다 +5초, 선택한 초기 시간 상한, 다음 선수 3초, 팀별 시작 포인트·효과음·음량 조절을 유지합니다.
- `경매 참가자 · 진행 현황` 표를 `순서 → 선수 → 포지션 → 낙찰 포인트 → 상태`로 표시합니다. 낙찰 확정된 행은 저장된 금액을 표시하고, 0포인트 낙찰은 `0 P`, 대기·진행·유찰은 `—`로 구분합니다. 재경매 때 이전 유찰 행과 새 낙찰 행의 금액도 구분합니다.
- 경매 바탕은 옅은 회색, 주요 패널·입찰 기록·팀·참가자 영역은 흰색으로 구분합니다. 라운지는 분홍 바탕과 흰 콘텐츠 영역을 유지합니다. 본문은 진한 남색, 보조 정보는 더 진한 회색, 작은 회원 정보도 13px로 조정했습니다.
- 정적 색상 계산에서 본문/흰색 15.96:1, 보조 글씨/흰색 6.29:1, 보조 글씨/회색 5.82:1, 흰 버튼 글씨/주요 빨강 5.10:1입니다. 이는 지정 색상의 계산이며 실제 브라우저 렌더링 전체에 대한 적합성 인증은 아닙니다. 대비 계산 (`test-artifacts/readability-contrast-report.json`)

**카카오톡 모집 명단 붙여넣기**

일반내전·경매·경기 전 재편성에서 쓰는 기존 `Riot ID 명단` 입력칸을 공통 개선했습니다. `1. 겨울#kr99 / d2 / ad mid`처럼 붙여넣으면 순번을 제거하고 첫 `/` 앞 ID로 등록 회원을 찾습니다. `8.경먀#kr1/d2/정글`처럼 띄어쓰기가 없어도 `경먀#KR1`로 추출합니다. `# kr1`의 공백과 태그 대소문자도 정규화합니다. 중복·미등록·미승인·모호한 항목은 미리보기에서 구분하고 정원 초과 추가를 차단합니다. 티어·포지션·접속 메모는 회원 DB를 덮어쓰지 않으며, 선택 후 기존 배정 포지션 표에서 확인·수정합니다.

**경매 시간·반응 속도 추가 개선**

- 진행 화면은 1초에서 0.5초 간격으로 갱신합니다. 본인의 입찰 버튼은 갱신 주기를 기다리지 않고 제출을 처리합니다. 응답이 늦으면 접수 확인 중 표시를 보여주고, 서버 확인 후 접수 결과를 표시합니다.
- 남은 시간은 화면 자료 조회를 모두 끝낸 뒤 계산하고 렌더에 걸린 시간을 차감합니다. 같은 선수·마감시각·단계의 새 응답으로 브라우저 카운트다운이 증가하지 않도록 했습니다. 정상 입찰 연장·새 선수·일시정지·재개는 각각 새 기준을 반영합니다. 실제 브라우저 전송 시간을 완전히 제거했다는 의미는 아닙니다.
- PostgreSQL 트랜잭션 초기 설정과 독립적인 목록 조회를 묶어 전송합니다. 설정을 마친 뒤 쓰기 잠금을 획득하고, 입찰에 불필요한 회원 점수·전적 집계와 이미 저장한 영수증의 재조회를 줄였습니다. 최신 권한 확인, 같은 읽기 스냅샷, DB 마감 판정·단일 정산은 유지합니다. [Psycopg의 공식 pipeline 설명](https://www.psycopg.org/psycopg3/docs/advanced/pipeline.html)

실제 Supabase에 개선 전·후 각각 58요청을 보냈고 최대 동시 요청은 16개였습니다. 아래 순차 API별 표본은 각각 20개이며 단위는 ms입니다.

| 측정 | 개선 전 p50 | 개선 후 p50 | 개선 전 p95 | 개선 후 p95 |
| --- | ---: | ---: | ---: | ---: |
| 입찰 접수 | 269.807 | 214.834 | 288.289 | 236.233 |
| 경매 조회 | 165.992 | 104.882 | 185.584 | 115.451 |

입찰 트랜잭션 중앙값은 208→145ms, 조회는 103→45ms로 줄었습니다. 동시 동일가 입찰 4건은 개선 전·후 모두 1건만 접수됐으며 잔액·원장·마감 검증과 임시 스키마 삭제 확인을 통과했습니다. 측정은 제어된 경매시계와 실제 DB 연결을 사용해 DB 시각 조회 1회가 생략된 조건입니다. 브라우저 렌더링·배포 환경의 응답시간 보장이나 동시 40명 지속 부하 검증으로 해석하면 안 됩니다. 개선 전 (`test-artifacts/auction-latency-baseline-remote.json`) · 개선 후 (`test-artifacts/auction-latency-after-remote.json`)

**최종 검증**

낙찰 포인트 열과 반응 속도 개선까지 반영한 최종 코드로 **전체 자동 테스트 366개가 모두 통과**했습니다. 2026-09-08 16:38:39~16:52:03 KST에 실행했으며 unittest 801.298초, 실행기 803.312초, 종료 코드 0입니다. 실행 중 소스·CSS·폰트·설정 변경은 없었습니다.

| 검증 | 결과·증거 |
| --- | --- |
| 전체 자동 테스트 | 366개 전부 통과, 종료 코드 0, 실행 중 파일 해시 변경 0개. 전체 로그 (`test-artifacts/db-improvements-tests.log`) · 실행 보고 (`test-artifacts/db-improvements-tests-report.json`) |
| Supabase 회원·일반내전 | 16단계 통과, 3개 일반내전·10개 경기·정정·탈퇴 복귀·원장 일치 확인, 임시 스키마 삭제. 결과 (`test-artifacts/member-normal-improvements-remote.json`) |
| Supabase v4→v5 | 7단계 통과, 기존 회원 10명·계정 2개·세션 2개·경기·점수·티어·내부 메모 보존, API 역할 접근 차단, 임시 스키마 삭제. 결과 (`test-artifacts/v5-upgrade-report.json`) |
| Supabase 가입·일반·경매 전체 | 17단계 통과, 회원 20명·팀장 4명·동시 동일가 1건 접수·중복 정산 방지·16낙찰·우승 업적 확인, 임시 스키마 삭제. 결과 (`test-artifacts/postgres-improvements-remote.json`) |
| Supabase 새 예외·유찰·명단 | 최종 18단계 통과, 동시 생성·미확인 입찰 조회·명단 확정·소개 편집 충돌·일시정지 재개·번호 붙은 명단 매칭·유찰 15명 동시 재시작, 임시 스키마 삭제 후 부재 확인. 결과 (`test-artifacts/db-improvements-final-remote.json`) |

원격 리허설 4개도 성능 변경 후 다시 실행해 **총 58단계 통과**했습니다. 준비·정리 단계가 포함된 수치이며, 검수용 스키마 4개의 삭제 후 부재와 관련 소스 해시 일치를 확인했습니다. 원격 재검증 묶음 (`test-artifacts/latency-release-remote-summary.json`)

이전 전체 실행에서는 360개 중 357개가 통과했고, 오래된 모의 서비스와 버튼 문구를 예상하던 테스트 3개가 실패했습니다. 모의 서비스는 새 접수 확인 흐름에 맞추고 일시정지 권한 검사는 안정된 버튼 키를 사용하도록 수정했습니다. 이후 시간·성능 보완과 회귀를 추가한 최종 366개 실행은 처음부터 끝까지 한 번에 통과했습니다. 이전 실패·중단 로그는 삭제하거나 성공으로 바꾸지 않고 별도 보존했습니다. 이전 전체 실행 (`test-artifacts/db-improvements-before-test-update.log`) · 테스트 수정 근거 (`test-artifacts/db-improvements-test-contract-audit.json`)

배포 검토용 [소스 ZIP](dist/rolymoly-source.zip)은 116개 파일로 구성하며 실제 비밀번호·DB·로컬 검수 로그를 제외했습니다. 검증 대상 96개 파일(그중 Python 88개)의 해시와 ZIP 안의 파일을 대조합니다. ZIP 사본에서 로컬 수신 주소 설정을 제거하는 배포용 차이만 허용합니다. 실행 진입점은 `app.py`입니다. 최종 코드·검증·ZIP 대조 결과 (`test-artifacts/db-improvements-verification.json`)

**실사용 관점의 변화와 제약**

- 모집 명단을 다시 타이핑하지 않고 붙여넣어 확인할 수 있습니다. 다만 명단에 있는 사람이 먼저 가입·승인을 완료해야 DB 회원과 연결됩니다. 미등록 ID를 임의로 회원으로 생성하거나 비슷한 닉네임에 자동 연결하지 않습니다.
- 모든 회원은 같은 개인계정을 사용하고 이번 경매의 팀장만 입찰합니다. 주최자는 경매마다 개설자로 정해지므로 일반 참가자·팀장·주최자가 고정된 별도 계정 유형으로 나뉘지 않습니다. 주최자 이름과 제어 버튼을 함께 표시해 누가 진행하는지 확인할 수 있습니다.
- 낙찰 금액·상태와 읽기 쉬운 입찰 기록을 함께 볼 수 있습니다. 진행 화면 표시 갱신은 0.5초 간격이며 마감 판정은 서버가 수행합니다. 즉시 이벤트를 보내는 웹소켓 푸시 방식이나 실제 사용자 접속 지연을 검증한 것은 아닙니다.
- 클랜 티어·현재 티어는 회원 화면에서 관리자가 수정하고 새 명단이 그 값을 사용합니다. Riot API 자동 갱신은 없으므로 현재 티어가 바뀌었을 때는 수정이 필요합니다. 이미 확정한 경기의 당시 티어는 기록 보존을 위해 유지합니다.

**남은 검수 범위와 대처**

- Cloud에 업로드·배포하지 않았습니다. 실제 여러 브라우저·기기에서 입찰 지연, 좁은 화면 배치, 폰트 로딩·음량 조작을 확인해야 합니다. 로컬 브라우저 검수 시도는 자동 승인 검토에서 이전 localhost 제외 요청을 근거로 거절됐으며 다른 경로로 우회하지 않았습니다.
- 접수 응답 유실은 미확인 요청 자동 확인으로 처리합니다. 미접수이면 현재 금액·마감을 확인하고 사용자가 다시 입찰합니다. 만료한 금액을 자동 재입찰하지 않습니다.
- 명단·회원·소개 편집 충돌은 최신 값 재확인 후 다시 제출합니다. 탈퇴로 참가 흐름이 막히면 주최자·관리자가 일시정지하고 복귀 또는 내전 중단을 판단합니다. 시작한 경매의 팀장을 임의로 바꾸지 않습니다.
- 현재 마감 워커는 앱 프로세스에서 동작합니다. 앱 휴면·종료 중 독립 마감 처리와 전체 DB 백업·복원 자동화, 실제 Cloud 검수는 별도 남은 작업입니다. 경매 중 배포를 피하고 일시정지·상태 확인·재개 절차를 사용합니다.
