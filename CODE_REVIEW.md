# 전체 코드·사용 흐름 재점검

> 과거 원자료·일회성 스크립트는 사용자의 정리 요청으로 삭제되었습니다. 인라인 경로는 과거 이력 표기이며, 최신 근거는 [Riot 연동 보고](RIOT_API_REPORT.md)에서 확인합니다.

> 과거 단계별 검토 기록입니다. 최신 Riot·회원 프로필·경매 UI와 검증 결과는 [현재 통합 보고](RIOT_API_REPORT.md)를 확인하세요.

개인 가입과 내전 개설 권한 변경 이후의 현재 기준은 [CURRENT_FLOW.md](CURRENT_FLOW.md)입니다. 아래 수동 계정 연결·복구 미구현 등의 평가는 이전 버전의 검토 이력으로 보존합니다.

2026-09-08 Supabase 추가 검토: 저장소 선택을 실행기·화면·진단 도구에서 통일하고 PostgreSQL SQL 변환·자동 ID·transaction 잠금·DB 기준 경매 시각을 구현했다. 공개 API 역할은 운영 스키마를 읽을 수 없고 최초 관리자 설정은 비공개 CLI로 제한한다. DB 응답이 유실된 입찰은 같은 요청 ID로 재시도하여 이중 접수를 막는다. 실제 PostgreSQL 검증과 최신 전체 테스트 결과는 [운영 리허설 보고](REHEARSAL.md)를 기준으로 확인한다.

아래 159개 결과와 후속 **166개 통과(448.654초·종료 코드 0·49개 소스 해시 일치)**는 Supabase 이식 전 SQLite 감사 이력이다. 당시 실제 6계정 브라우저 리허설에서 동시 입찰·재로그인 중 화면 갱신 경로 오류, 상태 변경에 따른 경기 선택 이동, 숨긴 탭의 복귀 지연을 발견해 수정했다. 이 브라우저·SQLite 복원 결과를 원격 PostgreSQL 배포 검증으로 확대해 해석하지 않는다.

검토일: 2026-09-07. 이전 검증 결과를 그대로 재사용하지 않고 현재 앱의 실행 코드 27개 파일, 설정·정적 자산·소스 패키징과 테스트를 다시 확인했다. 기존 ZIP의 `review-source/`는 비교 자료이며 앱이 실행하거나 불러오지 않는다. 검증에는 임시 DB와 별도 브라우저의 체험 데이터만 사용했다.

## 결론

기능 추가·삭제 사이의 실제 결함을 발견하여 수정했다. 주요 문제는 오래된 대진 ID 재사용, 내부 서비스의 권한·원장 원자성, 실행기와 화면의 DB 경로 불일치, 자동 갱신 중 안내·팀 현황 유지였다. 일반내전의 세트별 ±10과 경매 우승 업적 분리, 입찰 +5초/선택 시간 상한/다음 선수 3초 정책은 유지한다.

출석·모임 일정의 현재 실행 경로는 없다. 과거 출석 열·감사 기록과 기존 모임 일정 저장 자료는 삭제하지 않으며, 참가 확정이나 대진 생성의 조건으로 사용하지 않는다. 멸망전은 현재 구현 범위 밖이다.

## 확인한 코드 범위

| 범위 | 확인 내용 |
| --- | --- |
| `app.py`, `roly/ui.py`, `app_pages/` 7개 | 진입·공간 전환·역할별 접근, 생성/참가/입찰/기록 이동, 삭제 기능 잔여 경로 |
| `core.py`, `member_records.py`, `demo.py` | 회원·계정 상태, 로그인 폐기, 전력·업적 원장, 공개 자료와 관리 자료, 체험 격리 |
| `competition.py`, `tournament.py`, `live_auction.py` | 일반내전·경매 상태 전환, 동시 입찰·낙찰, 재경매, 예산·인원·포지션, 이전 DB 호환 |
| `result_revision.py` | 결과 정정 미리보기·만료·동시 변경, 후속 대진 무효화와 원장 보존 |
| UI 보조 모듈·CCv2 컴포넌트 | 명단 편집, 대화상자, 입력 상태, 시계·소리·음량, 카드·팀 팝업 |
| `server.py`, 패키징·설정·정적 자산 | 서버 재시작, worker 수명, 저장 경로, 소스 ZIP의 DB·비밀값 제외 |

## 재현한 문제와 수정

| 문제 | 재현 범위 | 수정 |
| --- | --- | --- |
| 이전 스키마에서 동률 경기 정정·삭제 후 새 대진 ID가 재사용되어 기존 원경기 요청 키와 충돌 | AUTOINCREMENT 없는 임시 DB에서 동률→추가 경기→정정→다시 추가 경기 | 현재·보관 대진과 sequence보다 큰 ID를 할당하여 이전 경기와 분리 |
| 감사 기록·점수 보정 조회에 토큰을 생략하면 인증을 건너뜀 | 하위 서비스 API. 현재 관리 화면은 이미 토큰을 전달함 | 관리자 인증을 항상 적용하고 비로그인·일반회원·진행자는 거절 |
| 외부 transaction 연결을 전달하면 타 진행자의 내전·경매나 종료·보상 확정 후 결과를 변경할 수 있음 | 하위 Core API. 정상 UI의 상위 서비스 검증과 구분 | Core에서도 이벤트 존재·종류·소유권·진행 상태·보상 여부 검사 |
| 내부 원장 작업 실패를 호출자가 잡고 외부 transaction을 커밋하면 반쪽 경기·업적이 남음 | DB 실패를 넣어 서비스 합성 경로 재현 | 작업별 SAVEPOINT로 해당 작업 전체를 되돌리고 같은 요청 키의 정상 재시도 허용 |
| 예전 클랜 소개 DB를 동시에 초기화하면 중복 열 추가 오류 | 포스터 열이 없는 임시 DB의 동시 초기화 | 스키마 확인·변경·기본값 생성을 같은 transaction으로 처리, 기존 소개 보존 |
| 종료·취소된 체험 경매의 worker가 계속 남음 | 빈 DB·종료 상태·재시작·동시 ensure | 처리할 경매가 없는 일반 worker 종료 및 등록 해제. READY·일시정지·유찰 대기는 유지. 실행기는 상시 worker 유지 |
| 다른 작업 폴더에서 상대 데이터 경로로 시작하면 실행기와 Streamlit이 서로 다른 DB를 열음 | 다른 cwd에서 실행기를 호출하고 자식 프로세스 환경 확인 | 시작 시 절대 경로로 확정하고 동일 경로를 자식에게 전달 |
| 라운지의 일반내전 전체 기록이 이전에 보던 경매 탭을 열음 | 경매 기록→라운지→일반내전 전체 기록 | 일반내전 탭으로 지정하고 이전 이벤트 선택 해제 |
| 입찰 거절 설명이 다음 1초 갱신에 사라짐 | 잔액 초과 입찰 후 반복 갱신 | 같은 선수·계정에서는 다음 작업까지 유지. 계정·선수가 바뀌면 해제 |
| 전체 팀 보기를 열어 둔 동안 낙찰 명단·잔액이 오래된 값으로 남음 | 팀 팝업을 닫지 않고 실제 낙찰 대기 | 팝업 1초 자동 갱신, 닫기·페이지 이동 시 상태 해제 |
| 이전 숫자 입력 상태가 입찰 클릭과 함께 재전송되면 화면의 증액과 다른 금액으로 접수됨 | 화면 10P/수신 0P를 AppTest 위젯 상태로 재현 | 버튼에 현재 금액 표시, 등록한 표시 금액과 제출값이 다르면 접수 거부. 입찰·잔액·마감·요청 키는 바꾸지 않고 재확인 안내 |

중단된 경매에서 기록 화면으로 이동하는 경로를 보완했고, 이미 로그인한 회원에게 다시 로그인하라는 대신 필요한 권한을 알려준다. 가입 승인과 로그인 계정 발급이 별개라는 점, 회원 연결 변경 후 재로그인이 필요하다는 점도 화면에 명시했다.

## 흐름이 막혔을 때

| 상황 | 정상 대응 | 보존되는 내용 |
| --- | --- | --- |
| 경매 시작 전 명단·팀장 오류 | 사유를 남기고 참가 명단 다시 준비 | 같은 경매·회원 명단·이전 설정 감사 기록 |
| 팀장이 입찰할 수 없음 | 일시정지→관리자가 계정·회원 연결 확인→재로그인→재개 | 낙찰 명단·예산·남은 시간 |
| 중복·같은 금액·잔액 초과·마감 후 입찰 | 서버에서 거절 또는 동일 요청의 기존 접수 결과 반환 | 기존 최고 입찰·잔액. 거절 요청은 마감을 연장하지 않음 |
| 유찰 발생 | 남은 선수 진행 후 재경매 | 이전 유찰·입찰 기록 |
| 경기 전 취소 | 사유를 남기고 취소 | 명단·낙찰·감사 기록 |
| 일부 경기 후 진행 불가 | 관리자 중단 종료 | 실제 경기·일반내전 확정 점수·낙찰, 미진행 경기와 우승 보상은 추가하지 않음 |
| 우승 확정 전 결과 오류 | 영향 미리보기→정정→무효화된 후속 경기 재진행 | 원경기·VOID 기록·보관 대진 |
| 서버 재시작 | `run.bat` 또는 `python -m roly.server` | 저장된 마감·낙찰 기준으로 복구 |

우승 확정 후 자동 재정산은 지원하지 않는다. 이 경우 현재 서비스로 우승 기록을 강제로 수정하지 말고, 사유와 정정 대상·원장 영향을 확인하는 별도 운영 절차가 필요하다. 데이터 이상을 강제 SQL로 덮어쓰는 복구 버튼은 추가하지 않았다.

## 실제 사용자 관점

| 사용자 | 편한 점 | 남은 불편과 개선 방향 |
| --- | --- | --- |
| 운영진 | 한 명단 편집 방식으로 일반내전·경매 준비, 부족 포지션 확인, 단계별 확정, 사유가 남는 복구 | 회원 승인→로그인 계정 생성→회원 연결이 수작업. 초대·최초 비밀번호 설정 흐름을 다음 개선으로 권장 |
| 팀장 | 본인 팀·잔액·증액 버튼, 타이머의 최고 입찰자, 자동 갱신·음량, 전체 팀 조회 | 모바일에서 팀 현황이 입찰 패널보다 먼저 쌓임. 시간 제한이 있으므로 모바일은 입찰 패널 우선 배치를 권장 |
| 일반 참가자 | 회원·팀·결과 조회, 점수와 업적 분리, 입찰 가능한 역할 안내 | 일반내전 메뉴는 생성용이라 참가자가 결과를 보려면 경기 기록으로 이동해야 함. 본인 참가 경기 바로가기를 권장 |
| 첫 방문자 | 운영 데이터와 분리된 체험 가능 | 운영 DB가 있어도 새 세션은 체험 공간부터 시작함. 실제 운영용 진입은 운영 공간을 기본으로 하는 구성이 적합 |

첫 진입 문제는 운영 회원 1명이 있는 임시 DB에서 재현했다. 새 세션에는 운영 회원 대신 별도의 예시 40명이 표시되며 데이터가 섞인 것은 아니다. 현재 기본값은 변경하지 않았다. 운영용 기본 진입, 회원 초대, 참가자 신청 방식은 기존 흐름을 추가로 바꾸는 설계 사항이므로 이번 오류 수정과 분리했다.

모바일 실측은 390×844 화면에서 입찰 버튼 상단이 약 1,488px 아래에 있었다. 가로 넘침은 없지만 첫 화면에서 입찰할 수 없는 사용성 제약이다. 반면 전체 팀 요약 팝업은 내부 스크롤 없이 표시되고, PC에서 열린 채 낙찰이 발생해도 461px 높이를 유지했다. 아주 작은 화면·브라우저 확대에서는 글자 읽기와 한 화면 표시가 충돌할 수 있다.

## 검증 결과와 범위

- 최종 입찰 금액 불일치 보완까지 포함한 현재 코드의 전체 자동 테스트 **159개 모두 통과**, 369.421초. 최종 재실행 기록은 `test-artifacts/current-code-full-tests.log`이다.
- 앞선 검증에서도 전체 158개(395.574초)와 입찰 보완 관련 13개가 통과했다. 위 159개 결과는 마지막 코드 수정 이후 모든 테스트를 처음부터 다시 실행한 결과다.
- 실제 별도 Chrome에서 운영진·팀장·일반 참가자 흐름, 1440px PC·390px 모바일, 권한 안내와 가입 설명을 확인했다.
- 전체 팀 팝업을 연 상태에서 실제 10P 입찰의 자동 낙찰을 기다려 명단 변경을 확인했다. 팝업 높이·스크롤은 낙찰 전후 같았고 화면 예외가 없었다.
- 최종 금액 보완 후 새 브라우저 체험에서 `0 P`→증액→`10 P 입찰하기`→실제 최고 입찰 10P→낙찰 잔액 1,160→1,150P를 확인했다. 전체 팀 팝업은 재개방 없이 1/5→2/5로 갱신되고 높이 461px, 내부 스크롤 없음으로 유지됐다. `test-artifacts/bid-confirmation-browser-report.json`에 측정 결과를 보관한다.
- 브라우저 검수 경매는 검수 완료 사유를 남기고 취소했다. 테스트용 체험 데이터이며 운영 경기에는 입찰하지 않았다.
- 빠른 연속 클릭 시도에서 0P가 접수된 Chrome 사례의 정확한 원인은 미확정이다. 다만 오래된 숫자 상태 재전송은 별도 AppTest에서 재현·수정했다. 새 값을 입력한 직후 버튼 금액이 아직 바뀌지 않았다면 첫 클릭은 거절될 수 있으며, 표시 금액을 확인한 뒤 다시 누른다. 합법적인 첫 0P 입찰은 유지한다.

이 검증은 모든 실제 사용자 환경에서 오류나 지연이 없음을 보장하지 않는다. 인터넷 사용자 부하, 백그라운드 모바일 탭, 재배포 중 연결 중단은 로컬 테스트와 다르다.

## 운영 배포 전 남은 사항

운영용 첫 접속 기본값·계정 발급 편의성과 별도로, 외부 공개 전 최초 관리자 생성 접근을 제한하고 DB 영속 저장·백업과 마감 worker 실행 환경을 준비해야 한다. Community Cloud의 파일 저장과 휴면은 별도 제약이 있다. 세부 내용은 [DEPLOYMENT.md](DEPLOYMENT.md)에 정리했다. 이번 점검에서 GitHub 업로드·외부 배포는 하지 않았다.
