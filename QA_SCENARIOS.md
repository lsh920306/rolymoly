# 다양한 진행·오류 시나리오 검증

> 과거 원자료·일회성 스크립트는 사용자의 정리 요청으로 삭제되었습니다. 인라인 경로는 과거 이력 표기이며, 현재 최종 검증 근거는 보존했습니다. 최신 근거는 [Riot 연동 보고](RIOT_API_REPORT.md)에서 확인합니다.

2026-09-08. 회원 DB 통합과 로그인·일반내전·경매 전체 흐름을 검증합니다. 저장소는 임시 SQLite 또는 새로 생성한 Supabase 검수 스키마이며, 운영 명단·계정·전적은 검수에 사용하지 않습니다. 시간 경계는 제어 가능한 시계로 재현하며 실제 인터넷 지연 측정과 구분합니다.

## v6 Riot·개인 프로필·체험 명단 변경

이번 변경의 전체 결과는 [Riot 연동 보고](RIOT_API_REPORT.md)에 모읍니다. 실행 코드·테스트·정적 자료를 고정한 전체 회귀, 실제 Supabase 검수 스키마, 실제 Riot 공개 계정 조회를 구분합니다.

**회원 목록 추가 변경 전 전체 자동 회귀: 508개 전부 통과.** 2026-09-08 19:30:12~19:47:56 KST, unittest 1061.085초, 실행기 1063.469초, 종료 코드 0, 검사 대상 120개 소스의 실행 중 변경 0개입니다. 첫 전체 실행의 테스트 격리 오류 2건을 보완한 뒤 전체를 다시 실행한 결과입니다. [전체 로그](test-artifacts/riot-auction-verified-tests.log) · [전체 실행·해시](test-artifacts/riot-auction-verified-tests-report.json) · [격리 수정 검증](test-artifacts/riot-isolation-focused-report.json)

이후 회원 목록의 주·부 포지션 열 분리, 관리자 수정 버튼의 가로 배치, 관리자 입력·Riot 조회 구분을 반영했습니다. 관련 화면 회귀 **40개·100.988초**, 기존 백엔드 회귀 **8개·14.517초**, 추가 임시 SQLite 예외 검수 **7항목**을 통과했습니다. 최신 화면 검수 동안 대상 소스 변경은 없었습니다. 전체 508개를 새로 실행한 결과가 아니며, 새 회귀 1개를 포함한 현재 전체 테스트 수는 509개입니다. [최신 화면 로그](test-artifacts/member-settings-tests.log) · [최신 소스 해시](test-artifacts/member-settings-tests-report.json) · [백엔드 추가 검수 요약](test-artifacts/riot-auction-release-verification.json)

동일 백엔드의 실제 Supabase 리허설 60단계와 SQLite 27단계도 통과했습니다. 이후 사용자의 명시적인 요청에 따라 과거 테스트 데이터·산출물 552개를 영구 삭제했습니다. 정리 후에는 실행 소스·정식 테스트·패키지 구성과 파일 참조를 확인했으며 전체 테스트를 다시 실행하지 않았습니다. [영구 삭제 보고](CLEANUP_REPORT.md). 아래 과거 버전의 테스트 수와 혼용하지 않습니다.

추가 검증은 다음을 포함합니다.

- 회원 각 행의 상세 버튼과 라운지 카드가 같은 회원 ID의 프로필로 이동하며, 다른 공간의 이전 ID로 잘못 조회하지 않음.
- 명시적인 개인별 갱신만 요청하고 화면 진입·검색·반복 렌더는 조회하지 않음. 중복 요청·5분 제한·401/403·404·429·서버 장애의 기존 캐시 보존.
- 솔로·자유랭크를 같은 HTTP 1회에서 분리하여 저장. 조회 실패·이전 자유랭크 미조회·정상 언랭크 구분. 실제 전적·숙련도와 내전 점수·우승 업적 분리.
- 세션별 체험 DB와 운영 DB가 같은 키의 20/1초·100/120초 요청 한도를 공유하면서 회원·점수·조회값은 각자의 DB에 저장됨.
- `allow_demo` 기본 false, 배포 체험 조회 차단, 설정 종료·키 변경 시 이전 체험 워커 중지. 공유 DB 장애에서 저장된 프로필·입력 폼 유지.
- 제공한 22명과 공백·태그 보존, 숙련도 5개를 포함한 체험 DB v5 전환 시 이전 DB 유지, 회원 ID와 팀장 계정·20명 경매 연결, 임의 클랜 티어만 저장하고 현재 티어는 임의 생성하지 않음.
- 팀장 지정 → 설정 팝업 → 요약·시작, 취소·권한 만료·다른 창 동시 저장·진행 중 설정 변경 차단, 랜덤 선수 순서 보존.
- 정적 체험 자료 준비가 전체 실패하면 정상 파일을 유지하고, 일부 성공이면 성공한 ID만 갱신하여 기존 정상 캐시를 보존함.
- 경매 초기화의 주최자·관리자 권한, 낙찰 환불·고정 팀장·설정 유지, 무작위 새 순서, 동시 입찰 충돌·같은 UUID 재시도·감사 저장 실패 전체 취소·대진 이후 차단.
- 초기화 확인 창의 DB 장애에서 연결 정보가 노출되지 않고 안내가 표시됨. 초기화 전 기록을 구분하고, 한 번도 관측하지 못한 과거 입찰도 새 효과음으로 울리지 않음.

실제 브라우저 레이아웃·기기 음량·네트워크 지연 및 Cloud 배포는 AppTest·Node 컴포넌트 검증과 별도입니다. 이번 검증에서 localhost 브라우저 검수는 수행하지 않았습니다.

## 이전 v5 개선의 검증 상태

이번 변경의 최종 코드는 **전체 자동 테스트 366개 전부 통과**했습니다. 2026-09-08 16:38:39~16:52:03 KST, unittest 801.298초·실행기 803.312초, 종료 코드 0, 실행 중 파일 변경 0개입니다. 낙찰 포인트 열·500ms 갱신·지연된 타이머 응답 처리·PostgreSQL 통신 개선까지 포함한 결과입니다. 전체 로그 (`test-artifacts/db-improvements-tests.log`) · 해시가 포함된 실행 보고 (`test-artifacts/db-improvements-tests-report.json`)

성능 변경 후 실제 Supabase 리허설도 16·17·7·18단계, **총 58단계 통과**했습니다. 네 검수 스키마는 삭제 후 부재까지 확인했습니다. 별도 전·후 성능 비교는 각각 58 API 요청·최대 동시 16요청이며, 입찰 중앙값 270→215ms·조회 166→105ms입니다. 이는 제어 시계를 사용한 API 계측이고 실제 브라우저 화면 반영 시간은 아닙니다. [최종 변경·성능 보고](DB_IMPROVEMENTS_REPORT.md) · 원격 검증 묶음 (`test-artifacts/latency-release-remote-summary.json`)

직전 전체 실행의 357개 통과·기존 테스트 3개 실패와 중단 사실도 과거 검증 이력으로 남깁니다. 당시 원자료는 정리 요청으로 삭제했습니다. 최종 결과는 그 수치를 합산한 것이 아니라 위 366개 전체 재실행 결과입니다. 아래의 307개 및 v4 리허설 수치는 이전 소스의 검증 이력이며 최신 브라우저 검수 완료를 뜻하지 않습니다.

새 검증 범위에는 Supabase 미설정 시 운영 시작 중단, 개인계정의 운영 역할과 마지막 관리자 보호, 거절 후 탈퇴·복귀, 신청 메시지와 내부 메모 분리, 내전 생성 UUID 재시도, 명단 확정 버전 검사, 당시 티어 CSV가 포함됩니다. 경매 화면은 일시정지·재개 위치 유지, 현재 라운드가 끝난 뒤에만 유찰 재시작, 유찰 순서 재추첨과 3초 준비, 흰 입찰 카드를 기준으로 검증합니다.

별도 v4→v5 구조 추가 리허설 (`test-artifacts/rehearse_v5_upgrade.py`)은 새로 만든 QA 스키마만 사용해 7단계·6.875초로 통과했습니다. 기존 회원·계정·세션·원장·경기 당시 정보, 내부 메모 보존과 새 신청 메시지의 빈값, 생성 영수증의 FK·재시도·공개 API 역할 접근 차단을 확인했고 검수 스키마를 정리했습니다.

## 과거 v4 전체 회귀 결과

**당시 판정: 307개 검증 완료, 미해결 실패 0건.** v4 검증 묶음 (`test-artifacts/member-db-verification.json`).

2026-09-08 14:27:27~14:38:09 KST에 전체 307개를 실행했습니다(640.575초, 실행기 642.453초). 303개는 통과했고, 새 티어 열을 예상하지 않은 기존 회원표 테스트 4개에서 하위 검증 12건이 실패했습니다. 추가한 클랜 티어·현재 티어·LP 열과 미입력 표시를 기준으로 해당 공통 검증만 수정한 뒤 4개를 모두 재실행해 통과했습니다(14:39:54~14:40:00 KST, 실행기 6.265초).

전체 실행을 한 번에 통과했다고 기록하지 않습니다. 최초 실패를 포함한 전체 실행 보고서 (`test-artifacts/member-db-tests-report.json`)·전체 로그 (`test-artifacts/member-db-tests.log`)와 4개 재검증 보고서 (`test-artifacts/member-db-display-retest-report.json`)·재검증 로그 (`test-artifacts/member-db-display-retest.log`)는 정리 요청으로 삭제했으며, 여기에는 검증 경위를 이력으로 남깁니다. 두 실행 사이 수정 파일은 `tests/test_ui.py` 하나이며 실행용 앱·백엔드 코드는 변경되지 않았습니다. 각 실행 중 소스 변경은 없었고 당시 검증 묶음에 해당 소스 해시를 기록했습니다. 이후 v5 소스·배포 묶음의 검증과 혼용하지 않습니다.

그보다 앞선 276개 통과는 개인 가입·명단·경매 흐름 변경 시점의 결과입니다. v4의 307개 검증에는 회원 티어·로그인 연결·동시 수정·수기 보정 중복 방지를 추가했습니다.

## 과거 v4 회원·로그인·일반내전 리허설

실제 Supabase의 새 검수 스키마에서 **16단계·86.906초 통과**했습니다. 결과 JSON (`test-artifacts/member-normal-remote-report.json`), [재현 스크립트](scripts/rehearse_member_normal.py). 관리자 2명과 개인 계정 22명을 만들고, 일반 회원 주최자 2명이 10명·20명 내전을 운영했습니다. 정상 진행만 반복하지 않고 다음 변수를 섞었습니다.

| 흐름 | 검증 내용 |
| --- | --- |
| 가입·로그인 | 중복 요청, 반려→수정→승인, 같은 계정·회원 ID 유지, 승인 전 제한 |
| 카카오톡 명단 | 중복·미승인·형식 오류·애매한 이름의 명시적 거절, 잘못된 정원·포지션 저장 방지 |
| 일반내전 10·20명 | 서로 다른 회원 개설자, 첫 경기 전 명단 변경 2회, 경기 후 변경 금지, 20인 풀리그 6경기 |
| 정책·결과 | 기존 내전은 ±10 유지, 새 정책 ±25는 새 내전부터 적용, 잘못 입력한 승리팀 정정 |
| 회원·계정 수정 | 클랜·현재 티어·LP 저장/비우기, Riot ID 변경 시 로그인 아이디·회원 ID 유지, 과거 명단·경기 정보 보존 |
| 동시 회원 수정 | 같은 이전 정보를 수정한 관리자 2명 중 1명만 저장 |
| 수기 점수 보정 | 같은 UUID 2건 동시 요청·재시도에서 +7점·원장·영수증·감사 기록 각 1회. 다른 금액·회원·사유·관리자의 키 재사용 4경로 거절 |
| 탈퇴·복귀·복구 | 탈퇴 시 세션 폐기·로그인 차단, 같은 회원 ID로 복귀, 일회용 코드 소비 후 새 비밀번호 로그인·로그아웃 |

일반내전 3개 종료, 경기 기록 10개 중 확정 9개·무효 1개, 일반내전 우승 업적 지급 0개를 확인했습니다. 무효→새 요청으로 재기록은 지원되는 개별 경기 경로에서 검증했습니다. 내전 대진에 연결된 경기에는 별도의 무효·재경기 기능을 새로 구현한 것이 아니며, 해당 경기 결과는 정정 흐름으로 검증했습니다. 검수 스키마 정리도 완료했습니다.

## 과거 v2·v3에서 v4로 구조 변경한 기록

이전 v2·v3 모양의 **합성 데이터**를 각각 새 Supabase QA 스키마에 준비한 뒤 v4로 갱신하고 재초기화했습니다. **11단계·11.672초 통과**, 두 검수 스키마 정리 완료입니다. 각 경우 기존 회원 10명·계정 2개·세션 2개·경기 1개·점수 원장이 유지됐고, 새 수기 보정 영수증은 정상 동작했습니다. 미저장 과거 티어를 현재 값으로 채우지 않는 것과 공개 API 역할의 접근 거절도 확인했습니다. 결과 JSON (`test-artifacts/profile-upgrade-report.json`), 재현 스크립트 (`test-artifacts/rehearse_profile_upgrade.py`).

이는 앱의 테이블·컬럼 추가 호환성 검증입니다. 실제 구회원이나 시트 자료를 가져온 작업이 아닙니다.

## 과거 규모별 경매 리허설

합성 회원 40명이 개인 계정으로 가입·승인·로그인한 데이터를 기준으로 독립 검수 DB를 만들어 진행했습니다. 재현 스크립트 (`test-artifacts/rehearse_auction_variants.py`), 결과 JSON (`test-artifacts/auction-variants-report.json`).

| 조합 | 섞어서 검증한 변수 | 결과 |
| --- | --- | --- |
| 4팀·20명·5초·순위 결정전 | 16명 전원 유찰 → 무작위 재경매 → 16명 낙찰 → 결승·3/4위전 | 4경기 완료, 우승 5명 각 1업적 단위, 중복 지급 없음 |
| 6팀·30명·15초·토너먼트 | 일시정지 중 시간 보존 → 잘못된 낙찰 취소·환불 → 재경매 → 부전승 2건 | 실제 5경기 완료, 우승 5명 각 5단위, 부전승 점수·판수 없음 |
| 8팀·40명·30초·조별리그 | 8명 동일 금액 입찰 → 예산 초과·다른 경매 선수 입찰 거절 → 32명 낙찰 | 동가 8건 중 1건 접수, 13경기 완료, 우승 5명 각 25단위 |

세 조합 모두 전력점수는 유지되고 우승 보상 배치는 경매당 한 번만 기록됐습니다. 합계 367개 불변 조건을 확인했으며 당시 실행은 46.757초, 임시 DB 정리 완료입니다. 이 숫자는 동시 접속 40명의 실제 브라우저 부하 테스트를 뜻하지 않습니다.

## 과거 v4 Supabase 검수 스키마 실행

- 기본·경합 리허설 결과 (`test-artifacts/postgres-member-db-flow-remote.json`): v4 당시 고정 코드 17단계, 78.187초. 20개 개인 계정의 승인 전 제한과 승인 후 재사용, 서로 다른 일반 회원 주최자, 일반내전 ±10과 정정, 경매 랜덤 순서 보존, 4명 동가 입찰 중 1명 접수, 두 정산 작업 중 1회 처리, 3초 전환, 우승 확정을 검증했습니다.
- 예외 리허설 결과 (`test-artifacts/postgres-member-db-exceptions-report.json`): 11단계, 54.313초. 마감 순간 입찰·정산 2개·로그인을 동시에 실행하고, 중복 낙찰 취소의 환불·재등록 1회, 상태가 바뀐 정정 미리보기 거절, 중복 가입의 계정·회원 1쌍, 반려 후 재신청, 복구 코드 동시 사용 중 1건 성공을 검증했습니다. 신청 변경 뒤 오래된 승인·거절과 선수 교환 뒤 오래된 결과 입력도 실제 DB에서 거부되는 것을 확인했습니다. 재현 스크립트 (`test-artifacts/rehearse_pg_exceptions.py`).

두 리허설 모두 새 검수 스키마의 삭제까지 확인했습니다. 앱 저장소·서비스를 실제 PostgreSQL에 연결한 검증이며, 실제 사용자 브라우저에서 인터넷을 끊은 실험은 아닙니다.

현재 최종 회귀·원격 리허설 근거와 정리 명세만 작업 공간의 `test-artifacts`에 보관합니다. 삭제된 과거 원자료와 일회성 스크립트는 위 인라인 경로로 이력만 표시합니다. 보존 근거는 공개 앱 소스 ZIP과 분리하며 실제 계정 비밀번호·DB 연결값은 보고서에 포함하지 않습니다.

## 회귀 테스트에 포함한 변수

| 영역 | 확인하는 경우 | 근거 파일 |
| --- | --- | --- |
| 운영 저장소 | 기본 실행의 설정 없음·빈값·부분 입력 시 중단, 명시적 SQLite 개발 예외, 화면·워커 같은 대상 | `test_storage_config.py`, `test_storage_selection_ui.py`, `test_server.py` |
| 회원 DB | 티어·LP 저장·유효성, 권한, 계정 이름 동기화, 중복 Riot ID·감사 실패의 전체 롤백, 버전 충돌·탈퇴/복귀 ABA | `test_member_profile.py` |
| 회원 화면 | 목록·관리자의 공통 편집, 옛 편집 차단과 재확인, 가입 티어 유지, 비공개 메모 비노출 | `test_member_profile_ui.py` |
| 과거 정보 | 일반내전 10·20명·경매의 확정 시점 정보, 이름·티어 변경 후 결과 정정, 기존 NULL 보존 | `test_member_snapshots.py`, `test_member_profile.py` |
| 점수·업적 폼 재전송 | 실제 위젯 제출 상태 재전송, 저장 성공 후 응답 유실·동일 요청 재시도, 새 요청의 의도적 추가 반영 | `test_manual_adjustment_ui.py` |
| 개인 가입 | 같은 UUID 재전송, 바뀐 내용, 중복 아이디·Riot ID, 동시 가입, 중간 실패의 전체 롤백 | `test_personal_auth.py` |
| 승인·보완 | 본인만 수정, 반려 후 재신청, 오래된 관리자 승인·거절 차단, 최신 정보 재확인, 승인 전 개설·입찰 거절 | `test_approval_context.py`, `test_personal_auth.py`, `test_operating_login.py` |
| 회원 복귀·운영 역할 | 거절→탈퇴→보완 가능 상태 복귀, 승인 이력·점수 보존, 개인계정 역할 부여·연결 고정, 마지막 관리자 보호·동시 강퇴, 영향받는 내전·팀장 표시 | `test_account_lifecycle.py` |
| 신청 메시지·내부 메모 | 새 메시지 별도 저장, 과거 혼합 메모 비공개 보존, 본인 수정으로 내부 메모 덮어쓰기 금지 | `test_account_lifecycle.py`, `test_member_profile.py`, `test_approval_context.py` |
| 계정 복구 | 기존 세션 무효화, 재발급 시 이전 코드 폐기, 만료·사용 완료 코드 거절, 두 요청 중 1회 사용 | `test_personal_auth.py`, `test_ui.py` |
| 주최·팀장 권한 | 승인 회원의 개설, 자기 내전만 진행, 경매마다 달라지는 팀장, 관리자 입찰 우회 금지 | `test_auth_live_bid.py`, `test_roster_workflow.py`, `test_tournament_ui.py` |
| 명단 | 미등록·미승인·중복·형식 오류, 정원·포지션 부족, 다른 화면 동시 변경, 변경 후 원복된 경우의 옛 화면. 카카오톡 번호·슬래시 형식 20~40명과 순번 뒤 공백 없는 ID 추출 | `test_roster_matching.py`, `test_roster_workflow.py`, `test_roster_ui.py` |
| 내전 생성·명단 확정 | 같은 UUID의 생성 한 번·내용 결합, 생성 응답 유실 재시도, 미저장 편집·명단 버전 변경 후 확정 차단 | `test_creation_requests.py`, `test_creation_requests_ui.py` |
| 열린 대화상자의 세션·권한 | 로그아웃·역할 변경·탈퇴·계정 전환 후 이전 생성/명단 제출 차단, 타 주최자 명단 접근 차단, 회수된 관리자 권한으로 내부 메모 조회 차단 | `test_dialog_sessions.py` |
| 명단과 경기 | 첫 결과 전만 재편성, 오래된 결과 폼 거절, 첫 결과와 명단 변경의 두 선행 순서, 동일 결과 재시도 | `test_result_context.py`, `test_events_ui.py` |
| 시간·입찰 | 6개 시간 선택, +5초·선택 시간 상한, 마감 후·잠금 대기 중 마감, 중복 UUID·표시 금액 차이 | `test_live_auction.py`, `test_live_auction_ui.py` |
| 시간 표시·DB 통신 | 마지막 조회 후 서버 시간 계산, 렌더 경과 차감, 늦은 동일 마감 응답으로 시간 증가 금지, 정상 연장·새 선수·정지·재개 구분, pipeline 묶음 조회·미지원 환경 순차 처리·지연 오류의 외부 롤백 | `test_bid_recovery.py`, `test_auction_components.py`, `test_postgres_adapter.py` |
| 예산·정원 | 예산 초과, 전액 사용·0P 입찰, 만석 팀, 다른 팀·다른 경매 대상 거절 | `test_live_auction.py`, `test_live_corrections.py` |
| 낙찰 정정 | 관리자·일시정지 조건, 환불·재배정·취소, 현재 최고입찰 예산 보호, 오래된 미리보기·ABA, 동시 취소 | `test_live_corrections.py` |
| 응답·서버 오류 | 저장 성공 후 응답 유실 재시도, 원장·감사 저장 실패 시 전체 롤백, 동일 요청의 단일 반영 | `test_remote_failures.py`, `test_live_auction_ui.py`, `test_core.py` |
| 경기·업적 | 정책 변경 후 기존 정책 유지, 수기 보정 보존, 승리팀 정정, 동률 추가 경기, 우승 보상 1회 | `test_competition.py`, `test_result_revision.py` |
| 운영 중단 | 참가자 탈퇴, 경기 후 중단 종료, 마감 저장 후 서버 재시작, 기록 보존 | `test_tournament.py`, `test_server.py`, `test_security.py` |

## 이번에 확인하지 못한 실제 운영 조건

v4에서 수정했던 수동 점수·업적 재전송, 회원 수정 버전 충돌, 미저장 과거 티어 처리에 더해 이번 v5에서는 거절 후 탈퇴 회원의 복구 불가, 기존 계정 재연결 후 복구 코드 잔존, 신청 메시지와 내부 메모 혼용, 생성 재시도·명단 확정 경계를 보완했습니다. 구현 검증과 아래 실제 운영 조건은 구분합니다.

- 실제 PC·휴대전화의 동시 40명 접속, 8명 팀장 입찰 중 표시 지연과 DB 대기시간의 분포.
- 실제 브라우저 네트워크 단절·복귀, 브라우저 저장소 차단, 다중 탭과 탭 종료 뒤 재접속. 로그인 복원 AppTest는 브라우저 응답을 모의 처리합니다.
- 새 UI의 모바일 배치, 실제 브라우저에서의 음량·자동 재생 정책. 컴포넌트 테스트와 실제 화면 검수는 다릅니다.
- Community Cloud 휴면·재배포, Supabase 연결 장애, 운영 DB 백업의 실제 복원.

현재 경매 공유 화면은 0.5초 간격 조회입니다. 모든 사용자에게 0.5초 이내 전달을 보장하거나 웹소켓 푸시를 구현했다고 해석하면 안 됩니다. 늦거나 불명확한 경우 일시정지 후 서버에 저장된 상태를 확인하고, 같은 요청 ID 재시도·명시적 재개·감사 이력이 남는 정정으로 복구합니다.
