**DB 통합·추가 개발 연결성 검토 — 2026-09-08**

> 과거 원자료·일회성 스크립트는 사용자의 정리 요청으로 삭제되었습니다. 인라인 경로는 과거 이력 표기이며, 최신 근거는 [Riot 연동 보고](RIOT_API_REPORT.md)에서 확인합니다.

> 이 문서는 수정 전 결함 재현 기록입니다. 이후 사용자가 요청한 추가 개선으로 아래 6개 결함을 수정했습니다. 현재 적용 내용과 최종 검증은 [추가 개선 보고](DB_IMPROVEMENTS_REPORT.md)를 확인하세요. 아래의 ‘현재’와 ‘미적용’ 표현은 이 검토를 수행한 수정 전 시점을 뜻합니다.

DB를 하나로 통합한 방향과 회원 ID를 중심으로 연결한 구조는 유지하는 것이 좋습니다. 점검 범위에서 구글 시트와 DB를 섞어 읽는 경로, 회원 이름 변경으로 기록 연결이 끊기는 문제, 티어 변경이 전력점수를 잘못 바꾸는 문제는 발견하지 못했습니다. 다만 정상 경로 테스트로 드러나지 않았던 재시도·동시 편집·가입 상태 전이의 결함이 있어, 현재 코드를 오류 없는 배포본으로 판단하지 않습니다.

이번 검토에서는 제품 코드와 운영 DB를 변경하지 않았습니다. 임시 SQLite, 실제 Streamlit AppTest 위젯, 서비스 API로 재현하고 증거를 `test-artifacts`에 저장했습니다. 아래의 수정안은 아직 적용되지 않았습니다.

**DB 자료를 가져다 쓰는 경로**

| 소비처 | 실제 연결 | 판정 |
| --- | --- | --- |
| 로그인·가입 신청 | `accounts.member_id` → `members.id`, `registration_requests`에서 같은 계정·회원 연결 | 개인가입 후 이름·티어를 수정해도 로그인 아이디와 회원 ID 유지. 기존 세션의 표시 이름도 갱신 |
| 라운지·회원 목록·회원 상세 | 공통 `Core`의 회원 조회. 현재 기본점수와 점수 원장, 일반내전 전적, 업적 원장 집계 | 현재 회원값을 사용. 라운지 컴포넌트는 공개 필드만 전달하며 메모·계정 정보를 전달하지 않음 |
| 일반내전 생성·경기 전 명단 재편성 | `members`의 최신 정보 → `competition_players`에 당시 정보 저장 | 최신 회원 이름·티어·포지션·전력 반영 |
| 경매 참가 명단 확정 | 회원 원본을 다시 조회해 이름·티어·LP·포지션·전력 보존 | 확정 뒤 원본 회원을 수정해도 기존 명단·팀장 예산 보존. 확정 버튼의 오래된 화면 검사는 별도 결함 |
| 경매 입찰·잔액·팀 구성 | 해당 경매의 `competition_teams`, `competition_players`, `live_lots`, `live_bids` | 같은 회원이라도 경매별 팀장 지정으로 권한 판단. 회원 전력점수와 경매 포인트 구분 |
| 경기 결과·정정·회원 기록 | `competition_games` ↔ `games`·`game_players`; 점수는 `score_ledger` | 당시 이름·티어 유지. 일반내전 결과 정정 시 차액 반영. 무효 경기 공개 전적 제외 |
| 우승 업적 | `award_batches`·`award_ledger` → 회원 조회 | 경기 점수와 독립. 정정·종료 재시도에서 중복 보상 방지 |
| 클랜 소개 | 같은 `Core`의 `clan_profile` | 저장소는 일치. 동시 편집 UI 문제는 아래 참조 |
| CSV 다운로드 | DB 조회 결과를 파일로 출력 | 구글 시트 읽기나 별도 운영 원본으로 사용하지 않음 |

회원 원본과 당시 명단의 값이 다른 것은 의도된 동작입니다. 예를 들어 이름·클랜 티어를 변경하면 회원 목록과 다음 내전은 새 값을 사용하지만, 이미 확정한 내전과 경기 기록은 당시 값을 유지해야 합니다. 이 복사본은 구글 시트와 DB의 이중 관리가 아니라 과거 기록 보존용입니다.

근거: [공통 서비스](roly/ui.py:24), [회원 조회](roly/core.py:391), [명단 스냅샷](roly/competition.py:182), [회원 기록 조회](roly/member_records.py:25), [공개 클랜원 카드](roly/member_cards.py:191).

**재현된 결함과 수정 방향**

| 번호 | 우선도 | 문제·조건·영향 | 권장 수정 |
| --- | --- | --- | --- |
| R1 | P2 | 입찰 10P가 DB에 저장된 뒤 응답만 유실되면 요청 번호가 화면에 남음. 다른 팀이 20P 입찰한 후 같은 화면에서 30P로 올리면 옛 요청 번호와 다른 내용이라는 이유로 거절됨 | 미확인 요청의 번호·금액을 함께 보존하고 접수 결과부터 확인. 확인된 요청과 새로운 입찰 의도를 분리 |
| R2 | P2 | 참가 명단을 다른 운영자가 교체한 뒤, 교체 전 화면의 ‘참가자 확정’ 요청을 보내면 보지 않은 새 명단이 그대로 확정됨 | 명단 저장과 동일하게 확정에도 `expected_roster_token` 검사. 사용자가 마지막으로 본 버전을 요청에 고정하고 DB 쓰기 안에서 비교 |
| R3 | P2 | 가입 거절 후 탈퇴 처리하면 회원은 `KICKED`, 신청은 `REJECTED`. 로그인·재신청·관리자 복귀 승인이 모두 막힘 | 최초 승인과 복귀를 분리. 미승인 신청자는 보완 가능한 대기 상태로 복귀시키고, 승인 이력이 있는 회원의 복귀는 별도로 처리 |
| R4 | P2 | 관리자 발급 계정을 다른 회원에 연결해도 이전 비밀번호 복구 코드가 유효하고 표시 이름이 옛 회원명으로 남음 | 새 운영 방식에서 불필요한 회원 재연결 경로를 제거하는 것을 권장. 유지할 경우 같은 트랜잭션에서 복구 코드·세션을 폐기하고 표시 이름 동기화 |
| R5 | P2 | 일반내전·경매 생성이 DB에 저장된 직후 응답만 유실되면 같은 버튼·폼 재시도로 동일한 내전이 2개 생성됨 | 생성에도 요청 UUID와 처리 영수증 적용. 재시도는 기존 내전을 반환하고, 사용자가 새 내전을 만들 때만 새 요청 번호 발급 |
| R6 | P2 | 클랜 소개 수정 창을 열어 둔 사이 다른 관리자가 소개를 바꾸면 내 입력이 저장되지 않아도 성공 메시지가 나오고 창이 닫힘 | 편집 시작 시 값과 버전을 보존하고 위젯에 고정 키 부여. 저장 시 DB 버전 비교 후 충돌을 알리고 입력을 유지 |

R1은 DB의 중복 방지 검사가 정상 작동하는데 UI가 재시도를 새 입찰로 전환하지 못하는 문제입니다. 동일 10P 요청을 먼저 다시 보내 접수 영수증을 확인한 뒤 30P를 보내면 성공했습니다. 사용자가 이 내부 절차를 알아야만 회복되는 UI는 개선해야 합니다. 근거: [auction_ui.py](roly/auction_ui.py:42), [입찰 요청 검사](roly/live_auction.py:242), 재현 결과 (`test-artifacts/review-bid-recovery.json`).

R2는 명단 저장의 버전 검사는 있지만 다음 단계인 확정에는 없는 불일치입니다. 실제 AppTest의 이전 버튼 요청을 다시 전송해 `CAPTAIN_SELECTION`으로 넘어가고 교체 회원이 확정되는 것을 확인했습니다. 근거: [확정 서비스](roly/tournament.py:144), [확정 버튼](app_pages/auction.py:106), 재현 결과 (`test-artifacts/review-roster-confirm-stale.json`).

R3는 처음 거절된 신청과 활동 회원의 탈퇴를 같은 복귀 함수로 처리하면서 생깁니다. 기존 거절 검사를 단순히 지우면 보완·승인 절차를 우회하므로 적절하지 않습니다. 근거: [가입 승인·탈퇴](roly/core.py:415), [재신청](roly/auth.py:112), [관리자 복귀 화면](app_pages/admin.py:180), 재현 결과 (`test-artifacts/review-account-lifecycle-report.json`).

R4는 직접 가입한 개인계정에는 해당하지 않습니다. 개인계정의 회원 연결은 고정되어 있습니다. 다만 현재 관리자 화면에는 별도 회원계정 발급·재연결 기능이 남아 있어 접근 가능한 경로입니다. 근거: [회원 재연결](roly/core.py:328), [복구 코드 사용](roly/auth.py:208), [관리자 계정 관리](app_pages/admin.py:269), 재현 결과 (`test-artifacts/review-account-lifecycle-report.json`).

R5는 일반내전과 경매 각각 실제 생성 위젯에서 재현했습니다. DB 저장 직후 합성 연결 오류를 주입했을 때 이미 내전은 1개 생성돼 있었고, 같은 요청을 다시 제출하자 2개가 됐습니다. 일반내전은 제목·명단이 같았고 경매는 제목·시작 시각이 같았습니다. 제목을 유일값으로 제한하면 정상적인 반복 개최까지 막으므로 요청 번호로 중복을 구분해야 합니다. 근거: [일반내전 생성](app_pages/normal.py:34), [일반내전 서비스](roly/competition.py:217), [경매 생성 화면](roly/registration_ui.py:33), [경매 생성 서비스](roly/tournament.py:96), 재현 결과 (`test-artifacts/review-db-ui-retry-report.json`).

R6는 다른 관리자 변경이 없는 대조군에서는 정상 저장됐습니다. 변경이 겹친 실험에서는 native 위젯 제출에 내 소개 문구가 있었지만 저장 함수에는 다른 관리자의 소개가 전달됐고, 내 입력 미저장·성공 1개·경고 0개·수정 창 닫힘을 확인했습니다. 최신 DB 값을 매번 읽어 키 없는 폼의 기본값으로 넣는 방식과 변경 버전 검사 누락이 원인입니다. 근거: [소개 수정 폼](roly/lounge_ui.py:39), [소개 저장](roly/lounge.py:31), 재현 결과 (`test-artifacts/review-db-ui-retry-report.json`).

**구조와 사용성 개선 권장**

| 항목 | 현재 상태 | 개선 방향 |
| --- | --- | --- |
| 신청 메시지와 운영 메모 | 신청자가 쓰는 ‘운영진에게 전할 말’과 관리자의 ‘운영 메모’가 모두 `members.notes`. 대기 회원은 이 값을 보고 재신청으로 덮어쓸 수 있음 | 신청 메시지와 관리자 내부 메모를 분리. 현재 코드는 메모를 내부 전용으로 보장하지 않으므로 명칭과 열람·수정 권한을 일치시켜야 함 |
| 개인계정의 운영 권한 | 직접 가입 계정은 영구적으로 `member` 역할만 허용하며 관리자는 별도 계정 발급 필요 | 모든 사람이 본인 계정을 쓰는 방향에 맞춰 회원 연결은 고정하고 운영 권한만 별도 부여하는 구조 권장. 경매별 팀장 권한은 현재처럼 해당 경매에서 판단 |
| DB 조회 비용 | 경매의 1초 갱신마다 세션·실시간 경매·전체 이벤트를 각각 조회. 4팀 기준 실제 SQLite 호출 계측에서 연결 3회·SELECT 17회 | 필요한 경매 화면 데이터를 한 읽기 작업으로 구성하고 팀별 반복 조회를 묶기. 이후 변경 여부·변경분을 전달하도록 개선. 입찰 쓰기의 권한·잔액·마감 원자 검사는 유지 |
| 저장소 선택 | 잘못된 부분 설정·연결 실패는 중단. 설정이 없거나 모두 비어 있으면 로컬 SQLite를 허용 | 배포용 운영 모드는 Supabase 지정과 유효 설정을 필수로 하고 체험·테스트에서만 SQLite를 허용하면 설정 누락 혼동 감소. 현재 운영 DB가 잘못 선택됐다는 재현은 아님 |
| 당시 티어 표시 | 명단·경기에 스냅샷을 저장하지만 일부 참가자 표·경기 상세·CSV에서는 티어 열이 빠져 있음 | 필요한 소비처에 저장된 클랜 티어·현재 티어·LP를 추가. 현재 회원을 다시 조회해 옛 기록을 채우면 안 됨 |
| 탈퇴 전 영향 안내 | 활성 내전에 연결된 회원도 탈퇴 처리 가능. 이후 입찰·결과 입력은 승인 상태 검사로 막힐 수 있음 | 진행 중인 참여 내전과 팀장 여부를 먼저 보여주고 일시정지·중단·복귀의 후속 경로를 안내 |

메모 문제는 열람 정책이 확정되지 않았으므로 비밀 유출이라고 단정하지 않습니다. 다만 서로 다른 작성 주체가 같은 필드를 덮어쓰는 구조는 현재 소스에서 확인됩니다. 근거: [신청 메시지](app_pages/join.py:39), [운영 메모](roly/member_editor.py:54), [재신청 저장](roly/auth.py:134).

개인계정 운영 권한의 근거는 [core.py](roly/core.py:351)입니다. 단순히 역할 제한만 삭제하면 회원 승인·탈퇴와 관리자 권한의 관계가 바뀌므로, 계정 활성 여부·회원 상태·운영 권한을 함께 검토해야 합니다.

조회 비용 계측은 실제 코드 호출을 임시 SQLite에서 센 결과이며 Supabase 응답 시간이나 동시 접속 한계를 측정한 결과는 아닙니다. 현재 `postgres.connect()`는 호출마다 새 드라이버 연결을 엽니다. 1초 갱신 설정만으로 모든 사용자에게 1초 이내 반영을 보장할 수 없습니다. 근거: [경매 갱신](roly/auction_ui.py:194), [반복 팀 조회](roly/live_auction.py:586), [PostgreSQL 연결](roly/postgres.py:371), 계측 결과 (`test-artifacts/review-bid-recovery.json`).

**이번 검증 증거와 한계**

- `unittest discover -s tests -p test_member*.py -v`: **38개 통과**, unittest 실행 79.463초, 전체 실행기 81.250초. 회원 프로필·스냅샷·회원 UI·클랜원 카드·일반내전 리허설 관련 기존 회귀 테스트입니다. 로그 (`test-artifacts/review-member-link-tests.log`) · 실행 정보 (`test-artifacts/review-member-link-tests-report.json`)
- 새 독립 연결 시나리오: 회원 이름·티어·포지션·기본점수 변경 → 일반내전 기록·정정 → 실제 경매 서비스 16낙찰 → 대진·결과 → 준결승 정정으로 후속 2경기 무효 → 탈퇴·복귀 → 재경기·우승 확정. 당시 정보·예산·원장·업적 연결 확인. 결과 (`test-artifacts/review-member-result-connectivity.json`)
- 계정 연결 시나리오: 개인계정 이름·티어 변경 후 로그인 표시·목록·상세 일치, 티어 수정으로 점수 변화 없음, 개인계정 재연결 차단, 임시 DB 외래키 위반 0건. 동시에 R3·R4 재현. 결과 (`test-artifacts/review-account-lifecycle-report.json`)
- R1·R2는 각각 입찰 응답 유실과 오래된 명단 확정 요청으로 추가 재현했습니다. ‘기존 테스트가 통과했다’는 사실과 ‘새 예외 결함이 발견됐다’는 사실은 양립합니다.
- R5·R6 추가 소비처 시나리오: 실제 일반내전·경매 생성 재시도에서 각각 1→2개 생성, 클랜 소개의 단독 편집 정상 저장과 동시 편집 입력 손실을 비교. 세 오류 경로 모두 AppTest 예외 0개였으므로 화면이 실행된다는 검사만으로는 발견되지 않습니다. 재현 코드 (`test-artifacts/review_db_ui_retry.py`) · 결과 (`test-artifacts/review-db-ui-retry-report.json`)
- 실행 코드와 의존성에서 Google Sheets·gspread·googleapiclient·시트 읽기 경로를 검색했으나 발견하지 못했습니다. 과거 정책 추출 문서와 파일 다운로드는 실행 연동과 구분했습니다.
- 이전 검증 대상 Python **80개 파일의 SHA-256은 변하지 않았습니다**. 검토 재현 스크립트·결과·이 문서만 추가했습니다. 기준 비교 (`test-artifacts/review-source-baseline.json`)
- 이번에는 실제 운영 DB 재검사, Cloud 배포 검증, 여러 실제 브라우저의 동시 접속·모바일·네트워크 지연 측정을 수행하지 않았습니다. 기존 Supabase 격리 리허설 결과는 과거 검증으로 보존하며 이번 결과로 대체하지 않습니다.

권장 순서는 재현된 예외 흐름 보완 → 계정·메모의 소유권 정리 → DB 조회 비용 개선 → 실제 Supabase와 여러 브라우저에서 재접속·응답 유실·동시 작업 리허설입니다. 회원·경기·원장 테이블을 다시 하나로 합치거나 구글 시트를 되살릴 필요는 없습니다.
