# Riot 프로필·경매 화면과 초기화 검토

2026-09-08. `app.py` 기반 Streamlit 앱의 추가 변경 보고입니다. 기존 DB 개선판의 테스트 결과와 이번 변경의 검증 결과를 구분합니다.

## 적용 기준

- 클랜 티어는 관리자가 조정하는 기존 평가값입니다. Riot 현재 티어는 한국 서버 솔로랭크(`RANKED_SOLO_5x5`)와 자유랭크(`RANKED_FLEX_SR`)를 한 번의 랭크 응답에서 함께 읽습니다. 프로필 상단에 두 티어·LP·승패·승률을 나란히 표시합니다. 회원 목록과 내전 명단의 기존 현재 티어 기준은 솔로랭크입니다.
- 주력 챔피언은 최종 요청대로 **숙련도 상위 5개**입니다. 챔피언 아이콘·이름·숙련도 점수·레벨을 표시합니다. 숙련도 점수를 승률로 표시하지 않습니다.
- 경매 상단은 현재·다음 선수 순서와 화살표, 오른쪽은 음량·경매 초기화입니다. 왼쪽 팀은 이전·다음 아이콘으로 전환하고, 옆 전체보기 아이콘은 포지션별 팀 현황 팝업을 엽니다. 가운데는 선수·입찰자·최고가·시간, 잔액과 입력 포인트, 증가 버튼·입찰 버튼 순서입니다. [참고 화면과 구현 매핑](AUCTION_REFERENCE_MAPPING.md)
- 회원 표의 현재 티어와 경매 카드의 API 정보는 같은 회원 ID의 DB 데이터를 사용합니다. Riot 갱신이 기본점수·일반내전 원장·우승 업적·경기 당시 스냅샷을 변경하지 않습니다.
- 경매 준비는 팀장 지정 팝업 → 경매 설정 팝업 → 설정 요약·경매 시작 순서로 정리합니다. 시간·팀별 포인트는 시작 전에 저장하며, 진행 중인 경매에는 설정 폼을 표시하지 않습니다.

## 회원 목록의 관리자 설정

주포지션과 부포지션은 회원 목록의 별도 두 열로 표시합니다. 관리자는 각 회원 행에서 가로로 나란히 배치한 ‘프로필 상세보기’·‘회원 정보 수정’ 버튼을 사용합니다. 수정 팝업에서 주·부 포지션을 각각 선택하고 클랜 티어를 직접 입력하며, 변경 사유와 함께 DB에 저장합니다. 일반 회원에게는 조회 버튼만 표시합니다.

이 세 항목의 관리자 수정 기능은 기존 백엔드에도 있었고, 목록에서 바로 찾을 수 있도록 진입점을 추가했습니다. Riot 갱신은 현재 랭크·전적·숙련도를 갱신하며 관리자 설정을 덮어쓰지 않습니다. 같은 주·부 포지션, 비관리자의 직접 요청, 다른 관리자가 먼저 수정한 오래된 폼은 기존 서버 검증에서 거절합니다. 확정된 내전은 당시 정보를 유지하고 새 명단에 변경값을 반영합니다. 기존 체험 명단의 최초 예시 포지션·임의 클랜 티어는 보존하며 관리자 수정 후의 갱신에는 영향을 주지 않습니다.

추가 변경 후 화면 회귀 **40개·100.988초**와 백엔드 회귀 **8개·14.517초**, 임시 SQLite 예외 검수 **7항목**을 통과했습니다. API 응답 중 관리자 수정 보존, 승인 회원의 우회 수정 거절, 과거·새 내전 스냅샷 및 점수·메모 보존도 확인했습니다. 실제 HTTP·운영 DB에는 접근하지 않았고 임시 DB는 정리했습니다. 기존 전체 508개 실행 후 소스 3개 파일만 변경했고 최신 화면 검증 중에는 소스 변경이 없었습니다. 전체 테스트를 다시 실행한 것은 아닙니다. [화면 로그](test-artifacts/member-settings-tests.log) · [검증한 현재 소스](test-artifacts/member-settings-tests-report.json) · [종합 확인 결과](test-artifacts/riot-auction-release-verification.json)

## 키 입력과 사용

실제 `.streamlit/secrets.toml` 끝에 다음 입력 자리를 추가했고, 사용자가 키를 입력한 뒤 설정 로딩과 실제 API 응답을 확인했습니다. 기존 Supabase 연결 정보는 유지했습니다.

```toml
[riot]
api_key = "본인의 Riot API 키"
allow_demo = false # 배포에서는 false. 배포 전 로컬 리허설만 true.
```

Streamlit Cloud에서는 배포 앱의 Settings → Secrets에 동일한 `[riot]` 항목을 추가합니다. 실제 키와 `secrets.toml`은 Git·소스 ZIP에 포함하지 않습니다. 키는 서버의 `X-Riot-Token` 헤더에만 사용하며 브라우저에 전달하지 않습니다.

## 갱신과 오류 처리

- 회원 목록의 **각 회원별 ‘프로필 상세보기’ 버튼**과 라운지 회원 카드가 같은 프로필 페이지로 이동합니다. 전체 행 선택이나 공통 하단 버튼 방식은 제거했습니다.
- 프로필의 ‘갱신하기’는 해당 회원 한 명만 조회합니다. 일반내전·경매 준비 화면에서도 회원 한 명을 선택한 뒤 ‘이 회원 갱신’을 누릅니다. 화면 진입·검색·자동 재실행만으로 Riot 요청을 예약하지 않습니다. 수동 재요청은 회원별 최소 5분 간격이며, 대기 중에는 기존 저장값을 보여줍니다.
- 프로필 상단에는 소환사 아이콘·Riot ID·레벨과 솔로·자유랭크별 실제 티어·승패·승률을 표시하고, 하단에는 숙련도 상위 5개를 표시합니다. 조회하지 못한 항목은 미입력으로 두며, 실제 조회 결과 랭크가 없을 때만 언랭크로 표시합니다. 경기별 KDA·CS·챔피언 승률은 별도의 Match-v5 연동 범위입니다.
- 운영 정보는 Supabase의 `riot_profiles`·`riot_jobs`에, 체험 조회값은 세션별 체험 DB에 저장합니다. 계정 → 한국 서버 프로필 → 솔로랭크 → 숙련도 조회를 분리해 한도 대기 후 중단 지점부터 이어갑니다.
- 요청 예산은 DB에서 공동 관리하며 키별 **1초 20회, 120초 100회** 이내로 제한합니다. ASIA/KR 호출을 합산하는 보수적인 제한입니다. 429 응답은 `Retry-After` 동안 대기합니다. 다른 앱이 같은 키를 쓰는 요청은 미리 계산할 수 없으므로 서버 응답 제한도 처리합니다.
- API 네트워크 대기 동안 DB 쓰기 잠금을 잡지 않습니다. 경매 0.5초 조회는 캐시만 읽고, API 요청이나 자동 재갱신을 실행하지 않습니다.
- 401/403은 키 확인이 필요한 상태, 404는 계정/한국 서버 정보 미확인, 정상 빈 랭크 목록은 언랭크로 구분합니다. 실패 때문에 기존 티어를 언랭크로 덮어쓰지 않습니다.
- Riot ID를 바꾸면 이전 캐시·작업을 폐기합니다. A→B→A로 이름을 되돌리거나 변경 중 옛 응답이 도착해도 이전 계정 정보가 되살아나지 않도록 검사합니다.
- 서버가 멈추면 외부 조회도 멈춥니다. 저장된 작업은 앱 재실행 시 이어갑니다. 이 갱신 작업은 경매의 마감 처리 작업과 별개입니다.

## 체험 명단과 경매 설정

제공한 실제 Riot ID 22명으로 체험 명단을 교체했습니다. `경 먀`, `수 빈`, `정 현`의 공백과 `외로운청년`, `테토` 같은 한글 태그를 보존합니다. 체험용 포지션·전력점수·경기 결과는 예시값입니다. 현재 티어와 숙련도는 각 프로필에서 갱신한 실제 조회값을 사용합니다.

기존 체험 세션은 숙련도 5개 자료를 포함한 버전 5의 새 DB 파일로 전환합니다. 검증 후 사용자의 삭제 요청에 따라 구버전 체험 DB 50개는 보관본까지 영구 삭제했으며, 현재 버전 5 체험 DB·체험 원본·운영 회원은 보존했습니다. 22명 중 20명이 4팀 경매에 참가하고, 일반내전·체험 계정 전환도 계속 사용할 수 있습니다. 클랜 티어는 실제 현재 티어에서 +1 또는 −2 세부 단계로 최초 생성 때 한 번 무작위 지정합니다. 아이언 4~챌린저 범위를 벗어나면 경계값을 사용합니다. 현재 티어 미입력·언랭크인 회원에게도 임의의 클랜 티어를 저장하되, Riot 현재 티어는 임의로 채우지 않습니다. 운영 회원이나 이후의 개인별 갱신에는 이 규칙을 적용하지 않습니다.

운영 Supabase 설정과 Riot 키, `allow_demo = true`가 준비된 체험 공간에서만 실제 조회를 허용합니다. 배포용 기본값은 false이고 모든 체험·운영 요청은 동일 Supabase 요청 한도를 공유합니다. 별도 SQLite 개발·QA 환경에서는 실제 설정을 읽지 않습니다. `scripts/prepare_demo_riot.py`의 명시적인 1회 실행은 공개 티어·숙련도를 정적 체험 자료로 저장하며 운영 회원·경기·점수를 변경하지 않습니다. 앱 실행 때 이 자료를 읽는 동작은 API 요청이 아닙니다.

새 경매는 참가 명단 확정 → 팀장 지정 팝업 → 경매 설정 팝업 순서로 진행합니다. 설정 팝업의 입찰 시간·팀별 시작 포인트를 저장한 뒤 시작합니다. 이미 팀장이 정해진 체험 경매는 ‘경매 설정하기’부터 확인할 수 있습니다. 서로 다른 팝업에서 동시에 설정을 저장해도 이전 화면이 최신 설정을 덮어쓰지 못하게 버전을 검사합니다.

## 경매 조작과 복구

- 상단 순서 띠에서 현재·다음 선수를 확인합니다. 왼쪽 팀은 화살표로 순환하고, 옆 격자 아이콘으로 전체 현황을 엽니다. 다른 팀을 구경해도 입찰 계정의 팀은 바뀌지 않습니다.
- 잔액과 입력 포인트를 나란히 보여주고 증액 7개·금액 초기화·파란 입찰 버튼으로 조작을 모았습니다. 금액 초기화는 작성 중인 금액만 현재 최고가로 돌리며, 이미 접수된 입찰을 취소하지 않습니다.
- 음량은 오른쪽 상단의 스피커 아이콘·슬라이더·퍼센트로 조절합니다. 사용자가 소리를 켠 뒤 새로 접수된 입찰에만 반응하고, 0%에서는 무음입니다. 초기화 전의 미관측 입찰이 늦게 도착해도 새 입찰 소리로 재생하지 않습니다.
- **경매 초기화**는 주최자·관리자에게 제공하며 확인 창에서 사유와 환불 금액을 검토합니다. 참가자·팀장·예산·입찰 시간을 유지하고 팀장 외 배정과 낙찰 금액을 되돌린 다음 새 무작위 순서를 저장합니다. 준비 완료·진행 중·대진 생성 전 경매 완료에서 사용할 수 있습니다.
- 이전 입찰·낙찰 결과는 기록을 지우지 않고 보존합니다. 초기화·취소 전 입찰은 별도 접힌 목록으로 구분합니다. 상세 감사는 일반 화면의 반복 조회에 무거운 전체 자료를 싣지 않도록 별도 감사 원장에 저장합니다.
- 확인 창 이후 입찰·일시정지·배정 등이 바뀌면 오래된 초기화를 거절합니다. 연결 응답을 잃어 같은 요청을 재시도해도 환불과 순서 추첨은 한 번만 처리합니다. 다른 팀장에게 초기화 권한을 주지 않습니다.
- 부분적인 낙찰 오류는 일시정지 후 관리자의 낙찰 정정으로 바로잡습니다. 유찰만 남았다면 주최자·관리자의 유찰 재시작을 사용합니다. 대진·경기·우승 보상이 생성된 뒤에는 전체 초기화를 차단합니다.

경매는 수동 새로고침 없이 0.5초 주기로 DB 결과를 읽고, 화면 시계는 그 사이를 보간합니다. 이 값은 화면 반영 시간의 보장이 아닙니다. API 조회가 지연되는 동안 별도 입찰이 접수되는지 확인했고, 이번 Supabase 단일 계측은 203ms였습니다. 실제 다중 브라우저·Cloud 지연과는 구분합니다. Riot 프로필의 ‘갱신하기’는 외부 API를 요청하는 별도 기능입니다.

Streamlit 배포가 체험·운영 공간을 자동으로 만들어 주지는 않습니다. 이 앱이 체험용 SQLite와 운영용 Supabase 경로를 분리합니다. Cloud에서도 이 분기는 유지되며, 배포 Secrets의 `allow_demo = false`로 체험 공간에서의 추가 API 요청을 막습니다. 미리 저장한 공개 체험 자료를 읽는 것은 API 호출이 아닙니다.

## 검증

사용자가 입력한 실제 키로 계정·프로필·솔로랭크·숙련도 조회 **4개가 모두 성공**했습니다. 운영 DB를 변경하지 않고 임시 요청 제한 장부로 확인했습니다. 키·PUUID·전체 API 응답은 보고서에 넣지 않았습니다. [실제 연결 결과](test-artifacts/riot-live-readonly.json)

이후 제공한 22명도 명시적으로 조회했습니다. 최종 솔로·자유랭크·숙련도 5개 조회는 **21명 성공, `야수#테토` 1명 `not_found`**이며 46.703초·85단계였습니다. 실패 회원은 현재 티어를 미입력으로 유지하고 체험 클랜 티어만 임의 저장합니다. 조회 성공 자료는 `static/demo-riot-profiles.json`의 공개 프로필로 저장했으며, 키·PUUID·비공개 응답은 포함하지 않습니다. [22명 준비 결과](test-artifacts/demo-riot-preparation.json)

추가 기능을 포함한 실제 Supabase 도메인 검수는 다음 **60단계**를 통과했습니다. 네 스키마 모두 새 검수 전용으로 만들고 삭제 후 부재를 확인했습니다.

| 검수 | 통과 단계 | 시간 | 근거 |
| --- | ---: | ---: | --- |
| 초기화·환불·동시 입찰·재경매 완주 | 12 | 93.922초 | [초기화](test-artifacts/auction-reset-rehearsal-remote.json) |
| 계정·회원 변경·일반내전 | 16 | 88.125초 | [회원·일반내전](test-artifacts/final-ui-member-normal-remote.json) |
| 경매·경기 결과·점수·우승 업적 | 17 | 70.735초 | [경매·결과](test-artifacts/final-ui-postgres-remote.json) |
| Riot 작업·캐시·요청 제한·DB 연결 | 15 | 74.562초 | [Riot](test-artifacts/final-ui-riot-remote.json) |

별도 SQLite 리허설은 초기화 12단계(24.250초), Riot 15단계(30.641초)를 통과했습니다. 가짜 Riot HTTP와 제어 시계로 429 재개, 키 교체, 20/1초·100/120초 한도, 느린 HTTP 중 입찰, 기존 회원·일반내전·스냅샷 보존을 확인했습니다. 실제 Riot 호출 22명 검수와는 별개이며 인터넷 지연이나 브라우저 반영 시간의 보장은 아닙니다. [초기화 로컬](test-artifacts/auction-reset-rehearsal-local.json) · [Riot 로컬](test-artifacts/final-ui-riot-sqlite.json)

원격 검수 도중 마지막 UI 오류를 수정했으므로 **원격 검수 당시 전체 소스와 최종 화면 소스가 같다고 주장하지 않습니다.** 원본 실행 묶음의 UI 변경 감지와 실패 상태를 보존했습니다. 대신 실제 검증한 백엔드 14개 파일의 전후·현재 해시가 일치함을 별도로 확인했습니다. [도메인 검수 통합](test-artifacts/final-ui-all-remote-summary.json) · [UI 변경을 기록한 원본 실행 묶음](test-artifacts/final-ui-other-remote-summary.json)

리뷰에서 다음 오류를 수정했습니다. 공유 DB 연결 실패가 프로필 진입·회원 선택 폼을 중단시키지 않게 했고, 체험 자료 재조회가 전체 실패하면 기존 파일을 유지하도록 했습니다. 초기화 팝업을 여는 순간의 DB 장애도 안전한 안내로 처리합니다. 초기화 전의 미관측 입찰이 늦게 전달되어 새 입찰 소리를 내던 경우는 취소된 lot을 음량 신호에서 제외해 해결했으며, 과거 입찰 표시도 분리했습니다.

전체 508개 첫 실행에서는 테스트 격리 오류 2건이 나왔습니다. 운영 로그인 화면 테스트가 남긴 Riot 워커 때문에 전역 개수 검증이 실패했고, Windows의 자식 프로세스가 AppTest가 바꾼 `__main__`을 이어받아 앱을 다시 실행했습니다. 제품 코드를 변경하지 않고 로그인 테스트의 외부 서비스를 차단하고, 워커 레지스트리와 실제 4개 프로세스의 진입점을 격리했습니다. 원래 오류가 난 순서 3개 및 관련 모듈 전체 37개(61.334초)를 재검증해 통과했습니다. [첫 전체 실행](test-artifacts/riot-auction-release-tests-report.json) · [격리 보완 근거](test-artifacts/riot-isolation-focused-report.json)

최종 전체 자동 회귀는 **508개 모두 통과**했습니다. 2026-09-08 19:30:12~19:47:56 KST, unittest 1061.085초·실행기 1063.469초, 종료 코드 0이며 실행 중 120개 대상 소스의 변경은 없었습니다. 실패·중단 실행의 통과 수를 합산한 값이 아닙니다. [최종 전체 로그](test-artifacts/riot-auction-verified-tests.log) · [소스 해시를 포함한 실행 보고](test-artifacts/riot-auction-verified-tests-report.json)

검증 후 보관했던 과거 자료 426개와 추가 검수 잔여물 126개를 사용자의 명시적인 삭제 요청에 따라 **총 552개, 35,631,635바이트 영구 삭제**했습니다. 구버전 체험·복구 DB, 임시 로그인 자료, 일회성 스크립트·CSV·캡처와 중복 로그가 대상이며, 보관 폴더도 제거했습니다. 현재 체험 원본·운영 데이터·최종 검증 근거는 보존했습니다. 이번 파일 정리에서는 운영 DB에 접속하거나 앱을 중지하지 않았습니다. [영구 삭제 내역](CLEANUP_REPORT.md)

실제 브라우저의 시각 검수·기기 음량·다중 접속 지연 및 GitHub·Cloud 배포는 이번 자동 검증과 별도이며 수행하지 않았습니다.

최종 전달 ZIP은 최신 회원 화면 검수의 120개 소스 해시, 포함 파일의 무결성, 실제 비밀값·DB·검수 자료 미포함 여부를 확인합니다. 삭제 명세와 대상 부재도 대조합니다. 전체 508개 테스트는 재실행하지 않았으며 추가 변경·검증 범위는 위 회원 목록 항목을 기준으로 확인합니다. [최종 소스·패키지·정리 확인 결과](test-artifacts/riot-auction-release-verification.json)

## 체험에서 확인하는 순서

1. 사이드바에서 체험 공간을 선택합니다. 새 버전의 제공 명단 22명과 예시 일반내전·경매를 불러옵니다. 기존 화면에 옛 명단이 남으면 앱을 다시 실행한 뒤 체험 설정에서 새 공간을 만듭니다.
2. 회원의 ‘프로필 상세보기’ 또는 라운지의 회원 카드를 누릅니다. 솔로·자유 티어와 숙련도 상위 5개를 확인하고, ‘갱신하기’로 그 회원 한 명을 갱신합니다.
3. 새 경매의 참가 명단을 확정하고 팀장 지정 → 경매 설정 → 시작을 확인합니다. 이미 준비된 예시 경매는 경매 설정부터 진행할 수 있습니다.
4. 사이드바 체험 계정을 팀장으로 전환해 입찰하고, 관리자로 돌아와 일시정지·재개·유찰 재시작을 확인합니다. 일반 참가자 계정은 함께 관전하지만 팀장 입찰 버튼을 사용할 수 없습니다.
5. 배포용 Secrets에서 `allow_demo = false`로 바꿉니다. 운영 회원은 가입·승인으로 준비하며 체험 원장·점수·임의 티어를 복사하지 않습니다.

## 공식 자료

- [Riot ID → PUUID 조회](https://developer.riotgames.com/docs/lol#obtaining-puuid-and-summonerid-from-riotid)
- [현재 랭크 API](https://developer.riotgames.com/apis#league-v4)
- [챔피언 숙련도 API](https://developer.riotgames.com/apis#champion-mastery-v4)
- [요청 제한·응답 코드·키 종류](https://developer.riotgames.com/docs/portal)
- [공식 챔피언 이름과 이미지 — Data Dragon](https://developer.riotgames.com/docs/lol#data-dragon)

개발용 키는 24시간마다 만료됩니다. 장기 운영에서 사용하는 키의 종류는 Developer Portal에서 확인해야 합니다. 현재 설정된 실제 키가 어느 발급 유형인지는 API 성공만으로 판정하지 않았습니다.
