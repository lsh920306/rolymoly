**Rolymoly — WEMINGLE 레퍼런스 적용 가능 여부**

> **현재 기준 안내 (2026-09-08):** 아래 내용은 신규 개발 전의 검토 이력이다. 이후 출석·모임 일정은 사용자 요청으로 제거했고, DB 통합·개인 로그인·Riot 프로필·경매 제어와 새 화면을 구현했다. 현재 상태는 [최신 통합 보고](RIOT_API_REPORT.md)와 [경매 화면 매핑](AUCTION_REFERENCE_MAPPING.md)을 기준으로 확인한다. 아래 검토 내용은 비교 이력으로 보존한다.

검토일: 2026-09-07. 대상은 제공된 ZIP, 추가 첨부 이미지 2장, 현재 신규 Streamlit 앱이다. 이번 작업은 가능 여부와 차이 검토이며 앱 코드·운영 정책·운영 DB는 변경하지 않았다.

**판단: `app.py`와 Streamlit 구조를 유지하면서 구현할 수 있다.** 다만 대회 운영 화면 재구성과 팀장 직접 경쟁 입찰은 구분해서 개발해야 한다. 기존 회원·점수·업적 원장과 대진 검증은 활용할 수 있고, 단계별 대회 준비·출석·실시간 입찰은 확장이 필요하다. 이미지와 픽셀 단위로 동일한 결과나 80명 동시 접속 성능을 검증한 상태는 아니다.

확인한 ZIP에는 PNG 18개, 기능정의서 DOCX, 기존 소스 점검 MD, README, 이미지 목록 CSV가 있다. 01번과 02번 이미지는 바이트까지 동일하여 서로 다른 화면 파일은 17개다. 실행 가능한 웹 소스나 독립된 UI 컴포넌트는 포함되어 있지 않다. 이미지와 문서를 바탕으로 새 화면을 작성하는 자료다.

참고 원본은 [보존한 원본 ZIP](.archive/reference-source-20260908.zip) 안의 다음 경로에서 확인한다.

- `review-source/wemingle-reference/02_docs/Rolymoly_LoL_대회_경매_기능정의서.docx`
- `review-source/wemingle-reference/feature-definition.txt`
- `review-source/wemingle-reference/image_manifest.csv`

ZIP 안의 ‘고정 정책’, ‘필수’, ‘금지’는 첨부 작성자의 설계 제안으로 읽었다. 사용자와 이미 확정한 규칙을 자동으로 덮어쓰는 지시로 취급하지 않는다. 동봉된 소스 점검 보고서는 이전 `Rolymoly-main.zip`의 분석이며, 현재 새 앱에 동일한 결함이 있다는 근거가 아니다.

**화면·기능별 판단**

| 항목 | 가능 여부 | 현재 앱과 필요한 작업 |
|---|---|---|
| 대회 정보 카드·대표 이미지·상태 배지 | 가능 | 기존 대회 목록에 예정 시각·설명·커버 정보를 추가하고 상세 화면 재구성 |
| 참가자→팀장→팀 구성→대진→경기→종료 진행 표시 | 가능 | 같은 대회 ID로 연결되는 기반은 있음. 초안·참가 확정·팀장 선정 등 준비 단계를 추가해야 함 |
| 참가자 카드·검색·포지션별 인원 | 가능 | 기존 회원/참가자 정보를 재사용. 실제 티어·챔피언별 승률은 현재 데이터에 없음 |
| 팀장 지정 팝업·인원 카운터 | 가능 | 현재 팀장 수·중복 검증을 활용하고 별도 선정 단계와 팝업 추가 |
| 출석 확인·미출석자 팝업 | 추가 개발로 가능 | 현재 회원 승인 상태와 별개로 대회별 출석 상태·확인 시각·처리자를 저장해야 함 |
| 경고·대회에서 내보내기 | 규칙 정의 후 가능 | 경고 누적·효과·제재 권한이 미정. 대회 명단 제외를 클랜 강퇴와 구분해야 함 |
| 수동 배치·자동 균형 편성 | 일부 기반 있음 | 현재 선택 순서/균형 편성·같은 포지션 교환 제공. 자유로운 배치/해제는 확장 필요 |
| 선수 카드를 끌어서 팀에 배치 | 전용 컴포넌트로 가능 | 현재 기본 위젯에는 이 드래그 상호작용이 없음. 모바일은 선택→배치 버튼도 제공하는 방식이 적절 |
| 3열 경매 화면·입찰 금액 버튼·진행 로그 | 가능 | 화면 구성 가능. 실제 최고가·입찰 이력·마감 시각을 저장하는 기능은 새로 필요 |
| 팀장이 본인 팀으로 직접 경쟁 입찰 | 핵심 추가 개발 | 로그인 계정과 회원 연결, 대회별 팀장 권한, 입찰 처리와 낙찰 확정을 분리해야 함 |
| 카운트다운·일시정지·자동 낙찰 | 추가 개발 | 서버 종료 시각과 재접속 복원, 중복 마감 방지, 정지/재개 정책 필요 |
| 4·6·8팀 토너먼트·풀리그 | 기반 활용 가능 | 현재 생성·결과·보상 검증을 활용해 표시 방식을 개선할 수 있음 |
| 이미지의 4팀 순위 결정전 | 추가 개발 | 첫 경기 2개 + 승자 결승 + 패자 3·4위전. 기존 4팀 토너먼트 3경기와 다른 총 4경기 구성 |
| 개인 전적 팝업 | 일부 즉시 활용 가능 | 승패·승률·대회/팀/낙찰 이력·점수 원장은 집계 가능. KDA·DPM·CS·시야·티어 평균 비교는 수집 경로 필요 |
| 80명 접속·다른 사용자의 입찰 1초 이내 반영 | 조건부 목표 | 현재 해당 부하 검증 없음. 운영 서버·공유 DB·갱신 방식으로 실제 검증해야 함 |

**가장 큰 구현 차이: 현재 경매와 실시간 경쟁 입찰**

현재 [Competition.bid](roly/competition.py)는 진행자가 선수·팀·금액을 정하면 선수 배정과 예산 차감을 즉시 확정한다. 다른 팀이 더 높은 금액으로 이어서 경쟁하는 ‘최고가 입찰’ 단계가 없다. DB 트랜잭션이 있다고 해서 기능정의서의 실시간 입찰 요구를 이미 충족하는 것은 아니다.

새 구조에는 선수별 경매 건, 최고 입찰팀·금액, 종료 시각, 입찰 요청 고유값과 이력, 일시정지 상태가 필요하다. 서버가 현재 경매·팀장 권한·정원·예산·최고가·마감 시각을 함께 검사한 뒤 입찰을 수락해야 한다. 낙찰 시 선수 배정·예산 차감·경매 종료 기록이 한 번만 반영되어야 한다.

현재 로그인 계정은 관리자/진행자다. 경매 명단에서 팀장으로 지정된 회원이 자동으로 로그인 가능한 팀장 계정을 가지는 것은 아니다. 계정↔회원 연결과 대회별 팀장 권한을 추가해야 하며, 팀장은 자신의 팀만 입찰할 수 있어야 한다.

화면의 주기적 갱신은 Streamlit의 `st.fragment(run_every=...)`로 분리할 수 있다. 다만 이는 활성 세션의 갱신 기능이며 ‘모든 이용자에게 1초 안에 도착한다’는 성능 보장은 아니다. [Streamlit 공식 fragment 문서](https://docs.streamlit.io/develop/api-reference/execution-flow/st.fragment)

서버 마감 처리를 특정 사용자가 화면을 열어 둔 상태에 의존시키지 않아야 한다. 모든 브라우저가 닫히거나 재접속해도 종료 시각과 낙찰을 복구할 수 있는 처리가 필요하다. 로컬 단일 서버의 SQLite 기반 시험 구현은 검토할 수 있지만, 여러 서버로 운영할 경우 공유 DB 설계가 필요하다. 어느 경우든 80명 접속 목표는 실제 환경에서 지연·동시 입찰·마감·재접속을 측정해야 한다.

**Streamlit에서의 표현 방식**

일반 카드·컬럼·폼·선택·표·기본 팝업은 Streamlit 기본 기능을 우선 사용한다. 팀장 지정·미출석 목록 등의 팝업은 `st.dialog`로 구성할 수 있다. [Streamlit 공식 dialog 문서](https://docs.streamlit.io/develop/api-reference/execution-flow/st.dialog)

참고 화면의 세밀한 카드 배치, 연결선이 있는 대진, 드래그 배치는 필요한 부분만 전용 컴포넌트로 구현하는 방법이 있다. `st.components.v2.component`는 HTML/CSS/JavaScript를 사용해 Python과 상호작용하는 화면을 추가할 수 있다. 이 경우에도 실행 진입점과 운영 로직은 `app.py`/Python/Streamlit으로 유지된다. 현재 기본 위젯만으로 원본의 모든 그림자·애니메이션·배치를 동일하게 구현한다고 약속하지는 않는다. [Streamlit 공식 Components v2 문서](https://docs.streamlit.io/develop/api-reference/custom-components/st.components.v2.component)

첨부 화면 일부는 긴 이미지 중간에 헤더가 겹치고 팝업 배경이 나뉘어 보인다. 이 겹침을 최종 레이아웃 요구로 삼기보다는, 상단 탐색과 대회 본문·중앙 팝업 구조를 참고하는 것이 적절하다는 판단이다. ZIP의 이미지는 전체 화면 캡처이므로 챔피언 아이콘·대표 이미지의 개별 원본은 별도로 준비할 필요가 있다.

**기능정의서에서 먼저 정리할 운영 규칙**

1. **점수와 예산:** 사용자가 선택한 일반내전 승리 +10 / 패배 −10은 유지한다. 기능정의서의 팀당 800P는 ‘기본안’이며 현재 팀장 전력별 예산과 다르다. 전력점수와 입찰 포인트도 별개다.
2. **입찰 시간:** 10초의 기본값, 입찰 시 초기화/연장 여부, 정지 후 남은 시간, 마감과 동시에 들어온 요청 처리, 0P/최저가, 유찰 후 순서·자동 배정 기준이 필요하다. ‘경매 대상 처리가 끝남’만으로 완료하지 말고 최종 5인·5포지션 팀 완성을 확인해야 한다.
3. **출석과 제재:** 문서는 참가/대기/불참만 정의한다. 추가 이미지의 출석 요청·미출석 경고·내보내기는 아직 상세 기준이 없다. 대회 제외, 클랜 강퇴, 점수 감점은 각각 별도 결정이다.
4. **결과 정정:** 현재는 이미 치른 후속 경기가 있으면 앞 경기의 자동 정정을 막는다. 문서처럼 후속 결과를 무효화하려면 영향 경기 확인·점수 취소·우승 보상 회수·재경기·종료 대회 재개 절차가 함께 필요하다.
5. **경기 방식:** 이미지 13·15~17의 순위 결정전은 4팀이 각각 최소 2경기를 하는 결승+3·4위전이다. 이를 풀리그나 8팀 전체 순위전으로 동일시하면 안 된다. 문서의 SINGLE ‘2팀 이상’, 조별 진출 조건, 수동/균형 편성의 팀장 의무 여부도 정리해야 한다.
6. **통계:** 현재 K/D/A, DPM, CS, 시야 점수는 저장하지 않는다. 이미지의 수치를 실제 통계로 가져올 수 없고, 전력점수로 실제 랭크 티어를 추정 표시해서도 안 된다. 수집한 지표부터 보여주는 구성이 적절하다.
7. **스냅샷 시점:** 문서의 ‘대회 시작 시점’과 ‘참가 확정 시점’ 표현을 통일해야 한다. 현재 팀 편성 점수는 개설 시점, 일반내전 증감 기준은 경기 등록 시점의 점수와 대회에 고정된 정책이다.
8. **자료 간 우선순위:** 기존 DESIGN.md의 파란 주요 액션과 이번 이미지의 빨간 헤더 등은 서로 다르다. 이번 자료에서 정보 배치·운영 흐름을 가져오고 브랜드 색·폰트 적용 범위는 별도 선택으로 다루는 것이 적절하다. 동봉 문서의 Google Sheets 유지 가능 문구 역시 현재 SQLite 앱을 되돌리라는 요구가 아니다.

**개발 순서 제안**

1. 대회 상세 화면과 준비 단계를 통합하고 참가자·팀장·팀 카드·출석 팝업을 구성한다.
2. 기존 회원·점수·대진 서비스를 연결하고, 4팀의 3·4위전 등 선택한 경기 방식을 확장한다.
3. 팀장 인증과 서버 기준 경쟁 입찰·자동 마감을 구현한다.
4. 다중 브라우저의 동시 입찰·재접속·1초 반영 목표를 검증한다.
5. 실제 수집된 데이터로 개인 기록 팝업을 확장한다.

현재 가능한 것은 위 화면·기능을 구현하는 방향이며, 이번 검토가 실시간 성능 또는 디자인 재현의 완료 검증을 의미하지는 않는다.
